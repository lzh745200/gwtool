# 公文汇编助手：功能盘点与增强方案

> 本文面向产品负责人与维护者，回答三个问题：**现在有什么**、**还缺什么**、**先做什么**。
> 所有判断均基于仓库代码与测试的实地核查，逐条附证据落点；不含凭印象的推测。
>
> 核查基线：v1.5.4，`gwtool/` 17,469 行 / 67 个模块，测试 799 通过 / 3 跳过，
> 双平台 CI（Windows x64 + 麒麟 ARM64）。

---

## 〇 本文的定位（先说清楚不做什么）

仓库里已有三份方案文档，**全部聚焦纠错引擎**：

| 文档 | 范围 |
| --- | --- |
| `纠错系统落地方案.md` | 引擎选型与量化路线 |
| `纠错引擎增强方案.md` | 四级流水线架构 |
| `纠错系统差距分析与增强实施计划.md` | D1–D9 缺陷清单 + P0–P3 实施计划 |

纠错方向已经规划到 P2（含「文内一致性校验」「数字规范扩容」「专名保护」等）。

**本文不重复上述内容**，转而盘点纠错之外的系统能力。凡与既有纠错方案有交集的
条目（如数字规范、文内一致性），本文会明确标注交集位置，并说明**为什么建议在
不同位置实现**——因为落点不同，收益结构与风险完全不同。

---

## 一 现状盘点：已经具备的能力

### 1.1 能力全貌

产品目前是**「材料进 → 加工 → 公文出 → 台账记」**的单向链路，覆盖 8 个板块：

| 板块 | 核心能力 | 关键落点 | 成熟度 |
| --- | --- | --- | --- |
| 材料收集 | 8 格式导入（docx/doc/wps/txt/rtf/pdf/md-html）＋ 内容嗅探路由 ＋ 扫描件 OCR ＋ 内容去重 | `core/importer.py`、`core/parsers/`、`core/ocr.py` | 高 |
| 写作辅助 | 12 种军队机关公文文种骨架 ＋ 文秘工具箱（金额/日期大写、简繁、全半角） | `core/skeletons.py`、`core/toolbox.py` | 高 |
| 汇编产出 | 三步向导 ＋ 模板引擎 ＋ A4 标准公文 / A3 骑马钉小册子 ＋ DOCX/PDF 双输出 | `core/compiler.py`、`core/docxgen.py`、`core/pdfrender.py`、`core/booklet.py` | 高 |
| 质量管控 | 五层纠错流水线（L1–L5）＋ 军队机关公文格式体检 ＋ 文档对比 ＋ 相似查重 | `core/corrector.py`、`core/repeat_rules.py`、`core/inspector.py`、`core/differ.py`、`core/simhash.py` | 高（纠错）/ 中（体检） |
| 资料管理 | 树形分类 ＋ 标签 ＋ FTS5 全文检索 ＋ 写作参考（三库 BM25）＋ 历史快照 | `db/dao.py`、`core/reference.py` | 高 |
| 发文管理 | 台账全要素登记 ＋ 发文字号自动取号 ＋ 组合筛选 ＋ 统计聚合 ＋ CSV 导出 | `core/registry.py`、`db/dao.py` | 中高 |
| 安全运维 | 口令锁（PBKDF2）＋ AES 加密备份 ＋ 附件体积上限与缺失清单 ＋ 退出自动备份轮转 ＋ 便携模式 | `core/security.py`、`core/backup.py` | 高 |
| 工程保障 | PyInstaller 双平台共用 spec ＋ 799 测试 ＋ ruff 门禁 ＋ e2e 17 步 ＋ 产物冒烟 ＋ 推理栈自检 | `gwtool.spec`、`tests/`、`scripts/` | 高 |

### 1.2 值得肯定的几个设计决策

这些是后续增强应当**继承而非推翻**的：

- **零网络、零新增依赖**是硬红线。连台账导出都刻意不引 `openpyxl`，用 CSV + UTF-8-BOM
  解决 Excel 中文乱码（`core/registry.py` 模块注释明确记录了这个取舍）。
- **平台差异收敛在两端**：运行依赖分档锁定（`requirements.txt`），打包参数唯一来源
  （`gwtool.spec`），中间层代码架构中立。
- **数据层单一入口**：所有读写走 `db/dao.py`，便于加审计、加索引、做迁移。
- **规则与数据分离**：纠错对按「来源」成组管理，可整体启停、CSV 双向导入导出
  （`core/ruleset.py`），这套机制可直接复用到其他规则型检查。

---

## 二 可增强之处：清单与实现方案

按**缺口性质**分四类。每项给出：现状证据 / 适用场景 / 实现方案 / 预期收益 / 成本与风险。

### A 类：业务闭环缺口（结构性，价值最高）

#### A1 收文管理完全缺失

| 项 | 内容 |
| --- | --- |
| **现状证据** | 全仓库 `grep '收文'` **零命中**；数据库 11 张表中只有 `dispatch_register`（发文），无任何收文表；`core/registry.py` 的 `STATUSES = ("拟稿","核稿","签发","已印发","已归档")` 是纯发文视角 |
| **缺口本质** | 公文实务是**收 + 发**两个方向。机关办公室的工作量里，**收文办理通常大于发文**（收文要拟办、批办、分办、承办、催办、归档）。当前产品只覆盖"我发出去的"，"别人发给我的"完全没有 |
| **适用场景** | 办公室文书岗、秘书岗日常收文登记；上级来文的办理跟踪；来文查询与统计（"去年某局发来几件、办结几件"） |
| **实现方案** | 新增 `receive_register` 表，字段设计**复用发文台账的模式**：<br>来文机关、来文字号、来文标题、文种、密级、紧急程度、收到日期、份数、拟办意见、领导批示、承办部门、承办人、办理结果、办结日期、归档号、备注<br><br>三个必须继承的纪律：<br>1. **共享常量**——文种、密级、紧急程度直接引用 `registry.py` 的既有元组，避免两处清单漂移（`registry.py:doc_types()` 已是这个做法）；<br>2. **扩展 `SCHEMA_VERSION` 3→4**，走既有迁移链（`db/schema.py` 已有版本迁移机制与迁移前自动备份）；<br>3. **来文字号另设解析**——来文可能是外单位字号格式，不能硬套本单位 `〔年份〕序号号` 正则，识别不出时允许自由文本 |
| **预期收益** | 补齐公文管理的另外一半，使产品从"写作工具"升级为"文书管理工具"；这是**同类竞品的标配**，缺失会直接导致采购评估失分 |
| **成本与风险** | 中等。新表 + 迁移 + UI 面板 + DAO + 测试，约 1 个模块+1 个面板的工作量。风险低（模式已验证）。**建议做基础版**（登记/查询/统计/导出），流转审批可后置 |

#### A2 办理时限与督办提醒

| 项 | 内容 |
| --- | --- |
| **现状证据** | `grep '待办'`「督办」「办理时限」均**零命中**；`registry.py` 只有 `URGENCY_LEVELS`（平急/急件/特急/特提）这一静态标记，无任何时间维度逻辑 |
| **缺口本质** | 有"紧急程度"却没有"时限"，等于记了严重性却不设闹钟。公文中**漏办、超期**是实质性失误 |
| **适用场景** | 特急件限期办结；领导批示件跟踪；临近到期提醒；超期汇总（可作为周报素材） |
| **实现方案** | 依赖 A1 的收文表（或为发文也加办理时限）。<br>1. 表增 `due_date`（应办结日期）与 `done_date`；<br>2. 新增 `core/reminder.py`：纯函数计算 `overdue` / `due_soon(N)` 两组清单；<br>3. **不做后台常驻进程**（单机桌面程序不应常驻）——改为**启动时扫描 + 主窗口状态栏常显角标**，点击展开清单；<br>4. 阈值（提前几天提醒、是否提醒发文）放 `settings` 表，用户可调 |
| **预期收益** | 把"记了台账"变成"管住时限"，是收文模块的价值放大器。**先有 A1 才有 A2** |
| **成本与风险** | 低（在 A1 基础上增量）。风险：提醒打扰——须默认克制（只提醒逾期与 2 日内到期），且可一键关闭 |

#### A3 归档与保管期限管理

| 项 | 内容 |
| --- | --- |
| **现状证据** | `归档` 一词在 `classify.py`/`dao.py`/`schema.py` 中出现，但全部是**资料库文档分类**语义，与「文书档案归档」无关；无保管期限、无档案号、无移交清单 |
| **缺口本质** | 按《机关文件材料归档范围和文书档案保管期限规定》，公文办结后须归档并标注**永久 / 30 年 / 10 年**。当前 `STATUSES` 里的"已归档"只是一个状态字符串，没有期限、没有载体 |
| **适用场景** | 年度归档整理、档案移交、到期鉴定销毁 |
| **实现方案** | 在 A1/A2 的表上增 `archive_no`（档号）、`retention`（永久/30年/10年）、`archive_date`；新增"年度归档"视图：按年度筛选 → 批量标注档号与期限 → 导出**移交清单**（复用 `report.py` 的 DocTree → DOCX 通路，自动继承公文字体版式） |
| **预期收益** | 覆盖文书工作"最后一公里"。价值真实但**触发频率低**（一年一次） |
| **成本与风险** | 低-中（复用台账与报告通路）。**优先级可后置**，因年度性需求不如日常需求紧迫 |

---

### B 类：质量管控深化

#### B1 引文规范校验（体检的明显空白）★

| 项 | 内容 |
| --- | --- |
| **现状证据** | `core/inspector.py` 全文**不含任何**「引」或「《」相关规则（`grep -n "引\|《" gwtool/core/inspector.py` 零命中）。该模块现有 **17 类检查**，逐类列出如下——**没有一类涉及引文**：<br>· 文本层 11 类：标题编号、标题层级、发文字号、成文日期、结束语、文种混用、请示规则、主送机关、篇幅、密级标注、附件说明<br>· docx 版式层 6 类：页边距、正文字体、正文字号、行距、标题字体、页码 |
| **缺口本质** | 「引用其他公文」是公文写作中**出现频率极高、出错率也极高**的环节，且规则明确、可机器校验——却恰好是体检唯一没覆盖的大类。注意现有体检**已经查了"发文字号"**（指本文自己的字号用不用六角括号），但**没有查"引用别人公文时的字号怎么写"**——两者是不同的规则对象，容易混淆，实施时须明确区分 |
| **适用场景** | 转发性通知、批复、报告中的依据引用；"根据《XX》……"句式；汇编材料中的交叉引用 |
| **实现方案** | 在 `inspector.py` 新增 `_check_citations(lines)`，复用既有 `Finding` 结构与 `report.py` 导出通路（零架构改动）。规则集：<br><br>**R1 书名号内应为全称**：`《……》` 中出现"简称""（试行）"叠用等异常<br>**R2 首次引用应带发文字号**：`《标题》` 后未跟 `（×政发〔2026〕5号）`——注意允许"已在上文标注过"的情形，故需按标题去重后只报首次<br>**R3 字号括号应用全角圆括号**：引用语境的字号用 `()` 还是 `〔〕` 与 `【R4】`：正文引用时外层用圆括号，内层才是六角括号<br>**R4 字号顺序**：`（×政发〔2026〕5号）` 中不得出现年份与序号倒置<br>**R5 引用语序**：检出"根据《X》根据《Y》"类同句重复引导词<br>**R6 引用已废止/变更机构**：与既有「机构沿革对照」（`corrector_data.py`，52 条）联动——这是**已有数据的新用法，无需新建数据**<br><br>严重度分级沿用既有三档：R1/R4 记 `error`，R2/R3/R5 记 `warn`，R6 记 `warn` |
| **预期收益** | 体检覆盖率显著提升；R6 让已有的 52 条机构沿革数据**从"纠错用"扩展为"体检用"**，一份数据两处收益。属于**低成本高感知**的改进 |
| **成本与风险** | **低**。约 100–150 行 + 规则测试 + e2e 增加一步。风险：R2 的"首次"判定需要文内状态，注意别对汇编成的长文重复报警（按标题去重即可） |

#### B2 数字用法规范扩容（GB/T 15835）

| 项 | 内容 |
| --- | --- |
| **现状证据** | `inspector.py` 现有数字相关规则只有 3 条（成文日期建议阿拉伯数字、月/日前导零、斜杠日期格式）；`纠错系统差距分析与增强实施计划.md` 的 D9 独立记录了"数字/单位/日期规范规则仅 3 条" |
| **与已有方案的交集** | D9 与 P1-7 已提出扩容，但**从纠错流水线视角**（作为纠错规则） |
| **本文建议的不同落点** | 建议**同时**在体检侧实现，理由：数字用法多数是**体例问题而非错别字**——"3 个"还是"三个"取决于语境（是否计量、是否概数、是否成语），误判为"错别字"会让用户对纠错失去信任；而作为"体检建议"（`info`/`warn` 级）更符合其性质，且能出现在可归档的体检报告里 |
| **实现方案** | 新增 `_check_number_usage(lines)`，按 GB/T 15835 与公文惯例分场景：<br>· 公历世纪/年代/年月日 → 阿拉伯数字<br>· **概数用汉字**（"三四个""十几个"，写成"3、4个"即错）<br>· **成语/惯用语内用汉字**（"一鼓作气""三令五申"，涉 `dictionary` 词表校验，避免误报）<br>· 计量数值 → 阿拉伯数字 + 法定计量单位<br>· 百分比、分数 → 阿拉伯数字（"50%""1/3"）<br>· 序数词视场合（"第一"与"第 1"）<br>规则措辞与严重度全部走 `info`/`warn`，**绝不报 error** |
| **预期收益** | 数字用法是公文质量评比的常见扣分项；作为体检项可直接产出整改清单 |
| **成本与风险** | 中。难点在**误报控制**（成语与概数），必须用词典仲裁——项目已有成熟经验（`repeat_rules.py` 的"词典仲裁"设计正是为此，可直接借鉴其判定思路） |

#### B3 文内一致性校验

| 项 | 内容 |
| --- | --- |
| **现状证据** | 无任何一致性检查；纠错流水线逐句处理，天然不感知全文 |
| **与已有方案的交集** | 纠错文档 P2-1 已列入（标注"提案未覆盖，公文最痛"） |
| **本文建议的不同落点** | P2-1 若走纠错流水线，会面临"纠错置信度"与"一致性提示"两种语义混用的问题。建议**归入体检**：一致性问题本质是"体例不统一"，输出物应是**整改清单**而非"替换建议" |
| **实现方案** | 1. **实体抽取**：人名（姓氏表 + 上下文）、机构名（复用 `corrector_data.py` 的机构沿革）、日期、数字、专有名词；<br>2. **变体归并**：判断两个写法是否"疑似同一实体"（如"张建国"与"张建军"、"县应急局"与"县应急管理局"）。用编辑距离 + 共同前缀，而非语义模型；<br>3. **输出**：`Finding("warn","一致性","「县应急局」与「县应急管理局」并存（各 N 次），请核对")` |
| **预期收益** | 命中**改稿后遗留**这一类最难自查的问题；长文档收益明显 |
| **成本与风险** | 中-高。**主要风险是误报**（"李强"与"李强华"可能是两个人）。策略：**只报并列关系不报对错**（提示核对，绝不自动替换），并给出各自出现次数与首次位置供人工判断 |

#### B4 公文用语与文风检查

| 项 | 内容 |
| --- | --- |
| **现状证据** | 纠错只处理"错别字与易混词"；`inspector.py` 不评价文风。全仓库无口语化/长句检测 |
| **缺口本质** | 公文有明确语体要求，而**口语化表达、超长句、生造词**是新人最常犯的问题，且这类问题**不像错别字那样能被规则表穷举**，需要另一套判定 |
| **适用场景** | 新人拟稿把关；对外发文前的语体自检 |
| **实现方案** | 规则为主、统计为辅，全部 `info` 级（**只提示不判定**）：<br>· **口语词表**：咱们/搞/弄/挺/贼/特别好 等（内部规范词库已有成组管理机制可直接承载）；<br>· **超长句**：按 `。；` 切分，>80 字提示（阈值可配）；<br>· **重复引导词**：连续段落均以"关于""为了"开头；<br>· **被动/虚词滥用**：连续"进行""加以"等<br>复用 `ruleset.py` 的「按来源成组管理 + 可整体启停」机制，让单位能维护自己的语体词表 |
| **预期收益** | 覆盖纠错与体检都够不着的"文风"维度，对**新人培养**价值高 |
| **成本与风险** | 低（纯规则表）。风险：主观性强 → 必须以 `info` 呈现且默认不打扰，避免变成"说教" |

---

### C 类：输出与互操作

#### C1 XLSX 导出（零依赖实现）★

| 项 | 内容 |
| --- | --- |
| **现状证据** | 全仓库无 `openpyxl`，导出只有 `registry.export_csv()`（UTF-8-BOM CSV）与 DOCX/PDF。`registry.py` 注释明确记录了"不引 openpyxl"的决策 |
| **缺口本质** | CSV 有三个**无法回避**的局限，而台账/统计恰恰都踩在上面：<br>1. **无格式**——列宽、冻结首行、数字格式全丢，用户拿到后每次都要重排；<br>2. **无多表**——统计结果（按文种/按机关/按月）只能塞进同一张表或分开多个文件；<br>3. **Excel 会误解数据**——发文字号等长数字串会被转成科学计数法，前导零丢失 |
| **关键判断** | 拒绝 `openpyxl` 是**对的**（依赖红线），但"不能引依赖"**不等于"不能出 xlsx"**——xlsx 本质是 **ZIP + 若干 XML**，`zipfile` + `xml.etree` 是标准库 |
| **实现方案** | 新增 `core/xlsx.py`，手写最小 OOXML：<br>需要的部件：`[Content_Types].xml`、`_rels/.rels`、`xl/workbook.xml`、`xl/_rels/workbook.xml.rels`、`xl/worksheets/sheet1.xml`（可多表）、`xl/styles.xml`（最小样式）<br>必须处理的坑：<br>· **XML 转义**（`&<>"` 与非法控制字符——公文里有 `〔〕《》` 属正常字符，但要防非法码位）；<br>· **长数字强制文本**：给发文字号单元格加 `t="inlineStr"` 或 `t="s"`，避免科学计数；<br>· **列宽**：`<cols>` 显式设定，否则默认窄得没法看；<br>· **日期**：写成文本（`YYYY-MM-DD`），比折腾日期序列号格式稳<br>落点：`registry.export_xlsx()`、`report` 统计导出，与既有 `export_csv` 并存（CSV 保留，兼容老用户与脚本场景） |
| **预期收益** | 台账、统计报表、体检报告、纠错清单**全部**可出 xlsx，是**一次性投入、多模块受益**的基础设施。且不增加任何依赖与安装包体积 |
| **成本与风险** | 中。手写 OOXML 有细节坑，**但风险可控**：测试用 `zipfile` + `xml.etree` 解析产物、断言单元格值与类型（不需要 openpyxl）；再补一条"Excel 能打开"的人工验收即可<br>**注意**：不要在运行期引入 openpyxl，也不要为了"省事"在测试里依赖它 |

#### C2 批量导出与移交包

| 项 | 内容 |
| --- | --- |
| **现状证据** | `backup.py` 是**整库**备份/恢复（含附件、模板、清单、轮转）；`batch.py` 是批量**汇编**（产出公文）。二者之间**没有"按范围导出资料"的能力** |
| **缺口本质** | 人员交接、项目资料移交、按专题归档时，需要"把某分类下的材料打包给出去"，而现在只能给整个数据库备份——**过度且不切实际**（含无关内容与隐私） |
| **适用场景** | 同事接手某专题；把某分类材料交给其他部门；按年度打包归档 |
| **实现方案** | 新增 `core/exporter.py`：按分类/标签/时间范围筛选 → 导出为**目录树 + `manifest.json`**（含文档元数据、原文件名、哈希）→ 可选是否含附件 → 打 ZIP。<br>**关键设计**：`manifest.json` 的 schema 与 `backup.py` 的 `_manifest_json` **保持同构**，使将来"从移交包导入"可以直接复用备份的校验与恢复逻辑，不另起一套 |
| **预期收益** | 打开"局部迁移"这条路；与整库备份形成互补（全量 vs 范围） |
| **成本与风险** | 低-中（筛选逻辑 DAO 已有，打包复用 `backup.py` 经验）。风险：附件体积不可控 → 复用既有的**附件体积上限 + 缺失清单**机制（`backup.py` 已实现，直接借用） |

#### C3 成稿随附材料来源清单

| 项 | 内容 |
| --- | --- |
| **现状证据** | `compiler.compile_docx()` 内部已有材料树（`load_trees`），但产出只有正文；汇编用的材料来源、导入时间、原文标题**不留痕** |
| **缺口本质** | 汇编类公文的**可追溯性**是审计与责任划分的要求——"这份汇编里的内容从哪来"必须能答。当前成品是一个孤立的 DOCX，来源信息只存在于操作者记忆里 |
| **适用场景** | 汇编成册、资料汇编、上级要求报送的材料合集 |
| **实现方案** | 在 `docxgen.generate_docx` 的既有版记/附件通路后追加一节「汇编材料来源清单」（表格：序号/原文件名/原文档标题/导入时间/分类），由 `compiler` 在汇编时自动收集并传入。<br>**做成可选**（向导里一个勾选框，默认勾选）——正式公文可能不需要，内部汇编材料则需要 |
| **预期收益** | 低成本补齐汇编的可追溯性；复用既有 DocTree→DOCX 通路，**无新依赖、无新排版逻辑** |
| **成本与风险** | **低**。约 60–100 行。风险：清单过长影响正文观感 → 可选 + 置于版记之后 |

---

### D 类：工程健壮性

#### D1 运行期诊断日志（当前最突出的工程缺口）★

| 项 | 内容 |
| --- | --- |
| **现状证据** | ① **全包 `logging.` 出现次数为 0**——整个产品没有一行结构化日志；<br>② **62 处 `except: pass/continue` 静默吞咽**，涉及 27 个文件（`backup.py` 10 处、`feature_dialogs.py` 5 处为最多）；<br>③ 纠错层有硬约束"任何异常必须咽掉"（`csc_neural.py`/`csc_gec.py`）——共 5 层，每层都可能静默失败；<br>④ 唯一的诊断输出是 `~/gwtool_启动诊断.log`，**只覆盖启动期**（架构/xcb 库检查） |
| **缺口本质** | 这个组合导致：**用户机上任何非启动期故障都是"不可复现、不可诊断"的**。用户能提供的信息只有"卡住了""没反应""纠错好像没生效"。而麒麟 ARM64 是**远程、离线、无法现场调试**的环境——这是最需要日志的场景，却一点都没有。<br>更关键的是：**纠错层静默降级是设计如此**，意味着"L4 实际没生效"与"L4 生效但没命中"在外部**完全无法区分** |
| **适用场景** | 用户报障；麒麟现场排障；CI/产物的行为验证；升级后回归对比 |
| **实现方案** | 新增 `gwtool/logs.py`，四条硬约束：<br><br>**① 非阻塞**：队列式 handler（`QueueHandler`+`QueueListener`），写盘在独立线程，**绝不卡 UI**；<br>**② 自身失败必须无害**：日志模块的任何异常全部吞掉（连"日志写不了"都不能让主流程挂），并回退为只打印到 stderr；<br>**③ 隐私红线**：日志**只记异常类型、模块、行号、堆栈摘要，绝不记公文正文**——这是涉密单位不可妥协的要求。可在日志中仅记录"处理了 N 字"这类计数，不记内容；<br>**④ 有界**：`RotatingFileHandler` 按大小轮转（如 2 MB × 5 份），落在既有 `paths.logs_dir()`，随备份轮转一并清理，**不允许无限增长**<br><br>接入点：<br>· `app.py` 安装 `sys.excepthook` + `threading.excepthook` + Qt 消息处理器，把**未捕获异常**落盘（当前这类异常在打包版双击启动时完全不可见）；<br>· `ui/workers.py` 的 6 类线程异常分支加 `logger.exception` —— 后台任务失败现在只弹个框，栈信息丢了；<br>· 62 处静默吞咽**不全改**：按"是否影响功能判断"分级，只给关键路径（纠错各层、导入各解析器、备份/恢复、数据库迁移）加**降级日志**（记一条"L4 未生效：原因 X"）。<br><br>同时把 `--runtime-report` 的输出并入日志，使每个用户机上都有**可对照的环境快照** |
| **预期收益** | 把"不可诊断"变为"看日志即可定位"。对**麒麟远程离线部署**这一主场景，收益是**数量级**的；同时让"纠错是否真在跑"第一次变成可查事实 |
| **成本与风险** | **低**（基础设施约 150 行 + 关键路径接入点若干）。风险有三，全部有解：<br>· *性能*：队列异步 + 有界轮转，实测可忽略；<br>· *隐私*：靠"绝不记正文"的编码纪律 + 加一条测试断言日志内容不含长文本；<br>· *磁盘*：上限 10 MB 封顶<br>**这是本文性价比最高的一项** |

#### D2 一键诊断包

| 项 | 内容 |
| --- | --- |
| **现状证据** | 无。排障依赖用户口述 + 手工截图 |
| **缺口本质** | 即使有了 D1 的日志，**让用户"把日志发回来"这个动作本身也有门槛**（找不到目录、不会打包） |
| **实现方案** | 依赖 D1。帮助菜单加「生成诊断包」：收集运行日志 + `--runtime-report` 输出 + 数据库表计数（**不含任何正文**）+ 系统信息（平台/glibc/Python/字体可用性/OCR 引擎路径）+ 已装增强包状态 → 打成一个 ZIP。**生成前明确展示将要包含的内容**，让用户放心 |
| **预期收益** | 把排障往返次数从"多轮问答"降到"一次一包" |
| **成本与风险** | 低（D1 完成后是纯组装）。**风险是隐私**：必须显式列出内容清单并允许用户勾选剔除 |

#### D3 数据库健康自检与压缩

| 项 | 内容 |
| --- | --- |
| **现状证据** | `db/connection.py` 有 WAL 与迁移前自动备份，但**没有完整性校验，也没有 VACUUM**。`backup.py` 的 `_validate_restored_db` 只在**恢复时**校验，日常运行不查 |
| **缺口本质** | 单机 SQLite 长期使用（大量增删文档、重建 FTS 索引）会产生碎片；异常断电可能损坏数据库。而**用户不会知道**，直到某天打不开 —— 此时自动备份轮转（保留 20 份）可能已经把损坏前的好数据挤掉了 |
| **适用场景** | 长期使用数月后性能下降；异常断电/强杀进程后 |
| **实现方案** | 1. **轻量自检**：`PRAGMA quick_check` 开销远小于 `integrity_check`，可在**距上次检查超过 N 天**时于启动后台线程跑一次，异常才提示（正常时完全静默）；<br>2. **手动维护入口**（设置菜单「数据库维护」）：`VACUUM` + `REINDEX` + 重建 FTS + 显示各表行数与文件体积前后对比；<br>3. **VACUUM 前置检查**：磁盘空间需 ≥ 2 倍数据库体积（VACUUM 会建临时副本），不足则明确提示而非中途失败 |
| **预期收益** | 提前发现损坏（在备份还能救的时候）；回收磁盘；消除"越用越慢" |
| **成本与风险** | 低-中。风险：VACUUM 期间需独占数据库 → 必须在无其他操作的时机执行并明确进度提示 |

#### D4 大数据量下的响应性

| 项 | 内容 |
| --- | --- |
| **现状证据** | `dao.list_documents(category_id, ...)` **无分页参数**；`library_panel` 一次性取全量填充列表。已知性能基线只有"50 文件导入 < 10s"与"检索 < 1s"，**没有列表渲染的大数据量基线** |
| **缺口本质** | 单机工具的资料库会**持续增长**（多年积累），而列表控件（`QListWidget`）在数千项时会出现明显卡顿。这是"用得越久越难用"的典型形态 |
| **适用场景** | 资料库超过约 3000–5000 篇文档的单位 |
| **实现方案** | 1. `list_documents` 增 `limit/offset`（SQL 层，改动小）；<br>2. UI 侧改**增量加载**：滚动到底部再取下一页，或加搜索框优先缩小范围（检索入口已有，正确的引导策略往往比虚拟列表更省事）；<br>3. 若确需一次性展示，再考虑 `QListView` + 自定义模型替代 `QListWidget`（工作量大，**建议先用分页验证是否真的需要**） |
| **预期收益** | 消除长期使用的性能衰减 |
| **成本与风险** | **风险是过早优化**——先加一条性能测试量化（构造 N 篇文档测列表构建耗时），**有数据再决定是否投入** |

---

## 三 优先级排序（价值 × 成本）

### 3.1 汇总表

| 优先级 | 编号 | 项目 | 价值 | 成本 | 依赖 | 一句话理由 |
| --- | --- | --- | --- | --- | --- | --- |
| **P0** | D1 | 运行期诊断日志 | **高** | 低 | 无 | 零日志 + 62 处静默吞咽，远程离线部署下**不可诊断**；性价比最高 |
| **P0** | B1 | 引文规范校验 | **高** | 低 | 无 | 体检唯一缺失的大类；规则明确；可复用已有机构沿革数据 |
| **P0** | B2 | 数字用法规范扩容 | 中高 | 低-中 | 无 | 沿用既有体检框架，公文评比常见扣分项 |
| **P1** | C1 | XLSX 导出 | 中高 | 中 | 无 | 一次投入多模块受益；零依赖；解决 CSV 三个硬局限 |
| **P1** | A1 | 收文登记（基础版） | **高** | 中 | 无 | 补齐公文管理另一半，竞品标配，采购评估要点 |
| **P1** | D3 | 数据库健康自检 | 中 | 低-中 | 无 | 在备份还能救的时候发现损坏 |
| **P1** | B3 | 文内一致性校验 | 中高 | 中-高 | 无 | 命中改稿遗留问题；须严格只报并列不判对错 |
| **P2** | A2 | 时限与督办提醒 | 中高 | 低 | A1 | A1 的价值放大器 |
| **P2** | C3 | 材料来源清单 | 中 | 低 | 无 | 低成本补汇编可追溯性，复用既有通路 |
| **P2** | D2 | 一键诊断包 | 中 | 低 | D1 | 把排障往返压到一次 |
| **P2** | C2 | 批量导出移交包 | 中 | 低-中 | 无 | 打开"局部迁移"，与整库备份互补 |
| **P2** | B4 | 用语与文风检查 | 中 | 低 | 无 | 新人培养价值；须克制不打扰 |
| **P3** | A3 | 归档与保管期限 | 中 | 低-中 | A1/A2 | 年度性需求，不紧迫 |
| **P3** | D4 | 大数据量响应性 | 中 | 中-高 | 无 | **先量化再决定**，避免过早优化 |

### 3.2 建议的执行节奏

**第 1 批（约 2 周，全部低风险）**——先补齐"看不见的短板"与"最明显的空白"：
> D1 诊断日志 → B1 引文规范 → B2 数字规范

选择理由：三项都不引入新依赖、不改数据库结构，可独立交付、独立回退；
且 D1 一次投入后，后续所有开发与排障都受益。

**第 2 批（约 3–4 周）**——投入产出比最高的功能增量：
> C1 XLSX 导出 → A1 收文登记 → D3 数据库自检

选择理由：C1 是基础设施（多模块复用）；A1 是最大的业务缺口；D3 补运维闭环。
**注意 A1 涉及 `SCHEMA_VERSION` 迁移，须与 C1 分开提交**，便于定位问题。

**第 3 批（约 4 周）**——价值放大器与体验补强：
> A2 时限督办 → C3 来源清单 → D2 诊断包 → C2 移交包 → B4 文风

**第 4 批（择机）**：
> A3 归档 → D4 大数据量（**先跑性能基线再定**）

---

## 四 明确不建议做的事

务实地排除，比列出更多功能更重要：

| 候选 | 为什么不建议 |
| --- | --- |
| **云同步 / 多人在线协同** | 直接违背"单机、完全离线、零网络请求"的产品定位。目标客户（党政机关、涉密单位）恰恰是**因为离线才敢用**。这不是技术难度问题，是定位问题 |
| **引入大模型做全文改写 / 摘要** | 与 L4 增强包**同源风险**，且已实测过：L4 在干净公文上误报达 67 处/千字，且会把正确的「部署」改成「谨骁」。在闸门达标前不应扩大此类能力；模型体积也会破坏安装包可控性 |
| **换 Electron / Web 架构** | 现有 PySide6 方案已解决双平台（含麒麟 ARM64）与离线分发，重写会丢掉全部平台适配成果（PySide6 版本分档、glibc 2.31 底线、xcb 预检、Droid Sans 兜底），收益无法覆盖成本 |
| **引入 openpyxl / pandas 等大依赖** | 违反依赖红线，且会波及 PyInstaller 产物体积与麒麟适配。xlsx 用标准库即可（见 C1） |
| **做后台常驻的定时提醒进程** | 单机桌面工具不应常驻。启动时扫描 + 状态栏常显已足够（见 A2） |
| **把纠错规则做成用户可编辑的正则** | 正则语法对目标用户（文书人员）门槛过高，且错误正则会导致大范围误报。既有"按来源成组管理 + CSV 导错对"的设计已经够用 |

---

## 五 与既有方案的关系（避免重复投入）

| 主题 | 既有方案 | 本文 | 关系 |
| --- | --- | --- | --- |
| 数字/单位/日期规范 | D9 / P1-7，**纠错流水线**视角 | B2，**体检**视角 | 建议以体检为主：这是体例问题而非错别字，误判为"错别字"会损伤纠错可信度 |
| 文内一致性 | P2-1，纠错视角 | B3，体检视角 | 同上；一致性问题应输出**整改清单**而非"替换建议" |
| 专名保护 | P1-4，纠错视角 | （不涉及） | 纠错侧已有规划，本文不重复 |
| L4 闸门 / 评测门禁 / dedupe | D1–D5，P0 全部 | （不涉及） | 纠错侧最高优先级，**不应被本文任何条目抢占资源** |
| 校对留痕 | P0-6 | （不涉及） | 纠错侧已有规划 |
| 长文本诊断 | 无 | D1 / D2 | 本文新增。**D1 同时服务于纠错侧排障**（让"L4 是否真在跑"可查） |

**资源分配建议**：纠错侧的 P0（L4 闸门等）是**正确性问题**，应当优先于本文任何条目。
本文的 P0（诊断日志、引文规范）可与纠错侧并行，因为**改动文件不重叠**
（`logs.py` / `inspector.py` vs 纠错各层），不存在冲突。

---

## 附 核查命令（可复核）

本文所有现状判断均可用以下命令复现：

```bash
# 1) 确认无收文相关实现（应无输出，退出码 1）
grep -rn "收文" gwtool/ --include=*.py

# 2) 确认全包零结构化日志（应输出 0）
python -c "import pathlib;print(sum(p.read_text(encoding='utf-8').count('logging.') for p in pathlib.Path('gwtool').rglob('*.py') if '__pycache__' not in str(p)))"

# 3) 统计静默吞咽异常的规模（应输出 62）
python -c "
import pathlib,re
t=0
for p in pathlib.Path('gwtool').rglob('*.py'):
    if '__pycache__' in str(p): continue
    t+=len(re.findall(r'except[^\n]*:\s*\n\s*(pass|continue)', p.read_text(encoding='utf-8')))
print('静默吞咽点:', t)"

# 4) 确认体检不含引文规则（应无输出，退出码 1）
grep -n "引\|《" gwtool/core/inspector.py

# 5) 列出体检的全部 Finding 类型（应输出 17 类，无引文相关）
python -c "
import re,pathlib
s=pathlib.Path('gwtool/core/inspector.py').read_text(encoding='utf-8')
print(sorted(set(m.group(1) for m in re.finditer(r'Finding\(\s*\"[a-z]+\",\s*\"([^\"]+)\"', s))))"

# 6) 确认运行期无 xlsx 依赖（预期仅命中 registry.py 注释中的 1 处提及）
grep -rn "openpyxl" gwtool/ --include=*.py

# 7) 确认 list_documents 无分页参数
grep -n "def list_documents" -A 3 gwtool/db/dao.py
```

> 注：第 5 条命令实测输出为
> `['主送机关', '发文字号', '成文日期', '文种混用', '标题字体', '标题层级', '标题编号', '正文字体', '正文字号', '篇幅', '结束语', '行距', '请示规则', '页码', '页边距']`
> ——共 15 类（另有「密级标注」「附件说明」两类因 `Finding(` 与参数跨行书写未被该正则捕获，
> 人工核对 `_check_classification` / `_check_attachments` 可确认存在），**合计 17 类，无引文相关**。

---

## 六 实施记录（2026-09-16 全部落地）

### 6.1 交付总览

| 编号 | 项目 | 新增/改动文件 | 测试文件 | 状态 |
| --- | --- | --- | --- | --- |
| D1 | 运行期诊断日志 | `gwtool/logs.py`（新）、`app.py`、`ui/workers.py`、`core/csc_neural.py`、`core/csc_gec.py`、`db/schema.py`、`db/connection.py` | `tests/test_logs.py`（19） | 完成 |
| B1 | 引文规范校验 | `core/inspector.py` | `tests/test_inspector_citation.py`（19） | 完成 |
| B2 | 数字用法（全文一致性） | `core/inspector.py` | `tests/test_inspector_number.py`（15） | 完成 |
| C1 | XLSX 导出（零依赖） | `core/xlsx.py`（新）、`core/registry.py`、`core/report.py`、`ui/registry_dialog.py`、`ui/feature_dialogs.py` | `tests/test_xlsx.py`（30） | 完成 |
| A1 | 收文登记管理 | `db/schema.py`（v3→v4）、`db/dao.py`、`core/receive.py`（新）、`ui/receive_dialog.py`（新）、`ui/main_window.py` | `tests/test_receive.py`（59） | 完成 |
| D3 | 数据库健康自检 | `core/dbhealth.py`（新）、`ui/main_window.py` | `tests/test_dbhealth.py`（26） | 完成 |
| B3 | 文内一致性校验 | `core/inspector.py` | `tests/test_inspector_consistency.py`（19） | 完成 |
| A2 | 时限与督办提醒 | `core/reminder.py`（新）、`ui/main_window.py` | `tests/test_reminder.py`（27） | 完成 |
| C3 | 汇编材料来源清单 | `core/compiler.py`、`ui/workers.py`、`ui/compile_wizard.py` | `tests/test_compiler_sources.py`（14） | 完成 |
| D2 | 一键诊断包 | `core/diagpack.py`（新）、`ui/main_window.py` | `tests/test_diagpack.py`（20） | 完成 |
| C2 | 批量导出与移交包 | `core/exporter.py`（新）、`ui/main_window.py` | `tests/test_exporter.py`（21） | 完成 |
| B4 | 公文用语与文风 | `core/inspector.py` | `tests/test_inspector_style.py`（14） | 完成 |
| A3 | 归档与保管期限 | `core/archive.py`（新）、`ui/receive_dialog.py` | `tests/test_archive.py`（22） | 完成 |
| D4 | 大数据量性能基线 | `scripts/bench_scale.py`（新） | ——（结论为"不实施"） | 完成（结论：无需优化） |

### 6.2 实施中的范围调整（三处，均有依据）

| 原方案 | 调整后 | 依据 |
| --- | --- | --- |
| B2 按 GB/T 15835 扩容数字规则 | 改为只做**全文一致性**（同一量词两种写法、百分比两种写法） | 核查发现纠错层 `NUMBER_RULES` **已覆盖**逐处规范（汉字年份、相邻概数、小数百分号）。在体检侧重复实现会让同一问题在纠错与体检两处各报一次 |
| B1 的 R6「引用已废止机构」 | **取消** | 纠错层 `corrector.py:76` 已用 `ORG_RENAME_PAIRS` 覆盖同一数据。体检侧再做属重复报警 |
| B4 含「重复引导词」检查 | 删除该项，只保留口语词与超长句 | 与 B1 的 C6（相邻「根据」未合并）重复 |

### 6.3 实施中发现并修复的**既有缺陷**（6 个）

这些缺陷在实现新功能的过程中才被暴露，**不改本次功能就永远发现不了**：

| # | 缺陷 | 危害 | 修复 |
| --- | --- | --- | --- |
| 1 | **备份恢复的向后兼容陷阱**（最严重） | `_validate_restored_db` 用 `required_table_names()`（由 `TABLES` 自动推导）判定"缺任意一张表即拒绝恢复"。于是**新增一张表会让用户所有旧备份立刻无法恢复** —— 升级后才暴露，且表现为"我的备份坏了" | 引入 `CORE_TABLES`：只有核心表（documents/settings/dictionary/error_pairs）缺失才拒绝；其余缺失属版本差异，恢复后由 `init_schema` 补空表。详见 `tests/test_backup_compat.py` |
| 2 | `dao.list_documents` 不返回 `file_path` | 移交包拿不到用户的原始文件，只能退化成导出的纯文本（原格式与批注全丢） | 把 `file_path` 加入查询列 |
| 3 | `dao.rebuild_fts` 返回 `dict` 而非 `int` | 曾误写 `int(dao.rebuild_fts())` → 抛错被宽 `except` 吞掉 → 表现为"维护成功但检索索引 0 条" | 正确汇总 dict 值并**补上降级日志** |
| 4 | 文风检查词表的前缀重叠 | 「咱们」与「咱」命中同一处各报一次，提示成倍放大 | 按词长降序处理 + `covered` 区间去重 |
| 5 | xlsx 无表头时数据从第 2 行开始 | 导出的表首行凭空空一行 | 起始行号按有无表头决定 |
| 6 | VACUUM 后体积"反增" | WAL 模式下 `-wal` 未归零，用户会以为维护把数据搞大了 | 统计前执行 `PRAGMA wal_checkpoint(TRUNCATE)` |

### 6.4 D4 性能基线（结论：**不实施优化**）

`scripts/bench_scale.py` 实测（临时库，含 FTS 索引重建与预热）：

| 文档数 | 入库 | 列表查询 | 含正文 | 检索（命中 50 条） | 库体积 |
| --- | --- | --- | --- | --- | --- |
| 300 | 0.00 s | 1.7 ms | 2.2 ms | 2.1 ms | <0.1 MB |
| 1000 | 0.01 s | 5.7 ms | 7.9 ms | 3.4 ms | 1.6 MB |
| 3000 | 0.02 s | 18.8 ms | 25.7 ms | 12.5 ms | 4.9 MB |

**结论：3000 篇规模下列表查询 18.8 ms、检索 12.5 ms，均远低于可感知阈值，
无需分页或虚拟列表。**方案里"先量化再决定"的要求由此得到落实 ——
省下了一次纯风险、无收益的 UI 数据流改造。

> 测量方法学提醒（本次踩到）：首发版本未做预热，得出"300 篇检索 733 ms、
> 3000 篇 0.3 ms"的**荒谬曲线** —— 差异全部来自 jieba 首次加载词典的固定开销。
> 脚本现已加预热，并在批量插入后重建 FTS 索引（否则是在空索引上测检索）。

### 6.5 剩余风险与未完成项（**如实说明**）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| 麒麟 ARM64 实机验收 | **未做** | 需真实机器，CI 与本机都不能替代（见 `麒麟ARM64完整实现方案.md` §五） |
| CI 流水线实跑 | **未做** | 仅本地门禁通过。CI 只在推 `v*` 标签时触发 |
| 安装包重建 | **未做** | 本次改动未重新打包；`smoke_dist.py` 需在打包后再跑一次 |
| 新增界面的**人眼验收** | **未做** | 收文台账、督办角标、诊断包、归档按钮均已通过离屏构造测试，但未做实际交互走查 |
| 日志的隐私纪律长期保持 | 有护栏 | `tests/test_logs.py` 断言 `redact()` 丢弃内容；但"调用方不把正文塞进日志"仍靠编码纪律 |
| 一致性检查的误报率 | 待观察 | 只报并列不判对错，且长度差 >4 字不猜；真实语料上仍需观察 |
| 文风词表覆盖面 | 刻意保守 | 只收几乎不可能出现在规范公文里的词；如用户反馈漏检，再按证据扩充 |

### 6.6 门禁数据（本地，终态）

| 项目 | 结果 |
| --- | --- |
| `ruff check .`（全仓库） | **All checks passed** |
| `pytest tests/ -q` | **1115 passed / 3 skipped / 0 failed**（229 s） |
| `scripts/e2e_check.py` | **20 项通过 / 0 项失败** |
| 测试基线变化 | 799 → **1118**（净增 319 条）；新增测试文件 **14** 个 |
| 改动规模 | 修改 28 个文件、新增 24 个文件；新增模块代码 **2222 行** |

### 6.7 发布记录（v1.6.0）

| 项 | 结果 |
| --- | --- |
| 版本号 | 1.5.4 → **1.6.0**（`gwtool/__init__.py`、`setup_windows.iss`、README） |
| 本地重建 | PyInstaller 2m16s → 集成 Tesseract → **冒烟 0 项失败** → 便携包 165MB → 安装包 124.7MB |
| 推送通道 | github.com 直连不通，改走 `api.github.com`（`scripts/api_commit.py --auto --tag=v1.6.0`） |
| CI 运行 | **run #77 全部成功**：build-windows / build-linux / package-linux / release |
| Release 产物 | [`v1.6.0`](https://github.com/lzh745200/gwtool/releases/tag/v1.6.0)：`gwtool_setup_win64.exe`（122.9MB）、`gwtool_1.6.0_linux_arm64.deb`（174.6MB） |

**CI 过程中修掉的 5 个问题**（前 4 次运行失败，全部修完才转绿）：

| # | 失败现象 | 根因 | 修复 |
| --- | --- | --- | --- |
| 1 | build-linux 第一步 `apt-get install ca-certificates` → **404** | Debian 11 已 EOL，bullseye-security 池中的 .deb 被撤下；而 `debian:11` 镜像又不含 ca-certificates | 改用归档/快照源（见 #3） |
| 2 | `archive.debian.org/debian-security` → **404 Release file** | 该路径**根本不存在**（实测探测确认） | bullseye-security 改指向 snapshot.debian.org |
| 3 | 证书鸡生蛋 | https 源需要 ca-certificates，而镜像里没有它 | **两个 apt 源都用 HTTP** + `[trusted=yes]`，一次装完 |
| 4 | build-linux「安装可选推理栈」仍失败 | 上一版把 ca-certificates 从安装列表移除了，导致 **pip 无法访问 https 的 PyPI** | 把 ca-certificates 装进系统（apt 仍走 HTTP） |
| 5 | **numpy 2.0.2 导致 onnxruntime 导入失败**（`_ARRAY_API not found`） | onnxruntime 1.17.3 的 ABI 绑定 NumPy 1.x，而其依赖声明无上界；**numpy 2.0.2 是有 cp39 轮子的**，所以"py39 会自然落到 1.x"的假设是错的 | 显式加 `numpy<2 ; python_version < '3.11'` |
| 6 | build-windows 测试 `access violation` 崩溃 | 数据库自检在 `MainWindow.__init__` 里启动后台线程，与"构造完立即操作界面"的测试竞争 | 改由 `showEvent` 触发（只启动一次） |

**共同的教训**：网络与依赖类问题**先探测再改**。前两轮靠推断 → 改 → 等 20 分钟 CI 验证，
代价高；第三轮先用 `http.client` 对 3 个候选 URL 各发一次 HEAD，一次命中。

### 6.8 过程中修复的一处**测试基础设施**缺陷（值得单列）

全量测试曾**永久挂起**：跑到 83% 停住，日志十几分钟不增一行、无任何报错。

**根因**：`test_ui_actions_smoke.py::test_all_menu_and_toolbar_actions_triggerable`
会遍历触发**所有 QAction**，而本次新增的「批量导出与移交包」用了
`QInputDialog.getItem(...)`。

**关键知识点**：PySide6 的**静态**方法（`QFileDialog.getOpenFileName`、
`QInputDialog.getItem` 等）是 C++ 侧实现，内部**不经过 Python 层的 `QDialog.exec`**，
所以 `monkeypatch.setattr(QDialog, "exec", ...)` **拦不住它们**，会真的弹出模态框。
该测试的 `win` fixture 已为 `QFileDialog` 的静态方法显式做了屏蔽，
**唯独漏了 `QInputDialog`** —— 本次的新代码把这个缺口暴露了出来。

**修复**：补上 `QInputDialog` 四个静态方法的屏蔽，并给该用例加
`@pytest.mark.timeout(180)`，使将来再出现挂起时是**失败报错**而非静默卡死。

> 定位手法（可复用）：① 对比日志大小两次读取是否变化，区分"卡住"与"慢"；
> ② `pytest --collect-only -q | head -n N | tail -20` 按收集顺序缩小范围；
> ③ `python -u -m pytest <单文件> -v > log` 看日志停在哪条（**`-u` 必加**，
> 否则重定向时行缓冲会吞掉线索）；④ 工程已装 `pytest-timeout`，可用 `--timeout=`。



