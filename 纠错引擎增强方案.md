# 纠错引擎增强方案

> ⚠️ **本文档为早期调研稿**。后续基于**真实代码库实测数据**完成的完整评估与落地方案，
> 见 [纠错系统落地方案.md](纠错系统落地方案.md)（含选型对比、量化方案、UI 设计、
> 语法扩展路径、技术栈分析、分阶段计划）。两份文档的核心结论一致：
> **不替换引擎，而是"加一层"L4 神经精排**。

> 适用版本：v1.5.0
> 定位：在**不破坏**本项目「单机离线 / 双平台（Win x64 + 麒麟 ARM64）/ PyInstaller 小体积」三条硬约束的前提下，
> 提升文字纠错的**准确率与覆盖面**。
> 结论来自对 `core/corrector.py`、`core/corrector_data.py` 的代码审计 + 对外部项目的许可证/体积/运行时核查。

---

## 0. 结论先行（TL;DR）

| 判断 | 内容 |
|------|------|
| ❌ 不要做 | 把 `pycorrector` / `ChineseErrorCorrector` 当**核心引擎整体引入**替换现有纠错。会同时撞碎「体积、平台、领域」三条线（见 §2） |
| ✅ 应该做 | 现有**四级加一**：保留三级流水线，**新增一层可选的神经精排（L4）**，且 L4 **不随主包分发** |
| 🥇 性价比最高 | 先补**词库与上下文规则**（零新增依赖、零体积、当天见效），再谈模型（§6 Phase 1） |
| 🔌 架构幸运点 | 现有 `Correction` 数据类 + `check_text()` 已是干净接口，L4 挂进去 **UI 层零改动** |

---

## 1. 事实核查（把外部项目的关键指标钉死）

| 项目 | 许可证 | 运行时依赖 | 模型体积 | 与本项目兼容性 |
|------|--------|-----------|---------|---------------|
| `shibing624/pycorrector` | **Apache-2.0**（可商用） | KenLM 路线需**本地编译 kenlm**；神经路线需 `torch + transformers`，官方建议 ≥8GB RAM、GPU 更佳 | 默认 KenLM 模型 `zh_giga...prune01244.klm` = **2.8 GB**；MacBERT4CSC 另计 | ❌ 主包不可承载（体积 + 编译依赖） |
| `TW-NLP/ChineseErrorCorrector` | 见仓库 | `transformers` / `llama.cpp` 等 | **4B 参数**（fp16 ≈ 8GB，4bit ≈ 2.5GB） | ❌ 麒麟 ARM64 桌面机不可行 |
| `houbb/word-checker` | 见仓库（Apache 系） | Java 运行时，**零模型** | 内置 **1397 条**中文错别字对照 + 27W 英文词 | ⚠️ 栈不兼容；但**词条数据可借鉴** |
| `Jacob-Zhou/simple-csc` | 见仓库 | 需本地跑大模型 | 大 | ⚠️ 体积问题同上 |
| `zstar1003/Localcheck` | 见仓库 | 轻量 | 小 | ✅ 可作**规则/检测补充**参考 |
| `taishan1994/awesome-chinese-text-correction` | 列表 | — | — | ✅ 仅作**资料索引** |

> 勘误：`pycorrector` 当前 star 数约 **7k**（上一轮口述的 5.6k 为其历史数据），仍是社区最活跃的中文纠错工具包。

---

## 2. 为什么不建议「整体替换引擎」（三条工程理由）

1. **体积红线**：KenLM 默认模型 2.8 GB，是本项目随包 `seed.db`（15.7 MB）的 **≈180 倍**。安装包从"几十 MB"直接变"几 GB"，与「开箱即用、便携 U 盘」的定位冲突。
2. **平台红线**：麒麟 CI 在 **Debian 11 容器 + Python 3.9** 内构建，为守住 glibc 2.31 底线不能升容器。`torch / transformers / kenlm` 在该组合下**没有稳定的 aarch64 wheel + 编译链**（本仓库 `requirements.txt` 顶部已专门记录过一次同类踩坑）。
3. **领域红线**：pycorrector 的混淆集来自 SIGHAN / 人民日报等**通用语料**，而公文有大量**固定搭配与法定用语**。通用模型在公文体上**误报率显著偏高**，恰好命中本项目最在意的"误报红线"。

> 一句话：**替换 = 拿 180 倍体积去换一批领域不匹配的召回，性价比为负。**

---

## 3. 现有引擎的真实短板（代码审计结论，非推测）

| 位置 | 现状 | 影响 |
|------|------|------|
| `corrector_data.py` · `CONTEXT_RULES` | **仅 3 条** | "同词不同境"（截止/截至、爆发/暴发）是公文高频错，这层容量几乎闲置 |
| `corrector_data.py` · `NEGATIVE_CONTEXTS` | **仅 1 条** | 负向上下文形同虚设，误报抑制主要靠词边界规则兜 |
| `corrector_data.py:110` | 存在自纠错死条目 `("恢复", "恢复", "错别字", 0.96)` | 被 `_is_valid_pair`（`wrong == correct` → False）**静默丢弃**，属无效数据，应删除 |
| `CURATED_PAIRS` | 221 条（其中 52 条机构沿革单独维护） | 精标规模偏小，是精度天花板的主因 |
| `corrector.py` · 低置信分支 | `conf < 0.7 且 jieba 词频 ≥ 30` 时**退回宽松规则** | 这是生成对（4 万条）**误报的风险敞口** |
| 整体 | 纯词表/规则命中 | **词表外的错字必然漏报**——这正是引入 L4 的唯一理由 |

---

## 4. 建议架构：四级流水线（加一层，前三级完全不动）

```
                         ┌─────────────────────── check_text(text) ───────────────────────┐
输入文本 ──► L1 精确词表 ──► L2 上下文/标点/数字规则 ──► L3 词边界保护 ──► [L4 神经精排] ──► list[Correction]
             (4 万对)         (3+9+3 条)              (jieba 分词)       (可选·opt-in)          (不重叠·已去重)
                                                                              │
                                                                    仅对"低置信/疑似"片段二次判定
                                                                    模型缺失 → 静默跳过，永不阻塞
```

**接入点唯一**：`corrector.py:check_text()`（L91）。L4 输出同样是 `Correction(start,end,wrong,suggestion,category,reason,confidence)`，
因此 `reference_panel.py`、`correct_dialog.py`、`to_marked_html()` **全部零改动**。

---

## 5. L4 的落地方式（关键：走 ONNX，不引 torch）

| 决策项 | 方案 | 理由 |
|--------|------|------|
| 推理载体 | **onnxruntime** | MIT 许可；有 `manylinux2014_aarch64` 官方 wheel（glibc 2.17 基线 → 麒麟 2.31 满足） |
| 模型 | MacBERT4CSC → 导出 ONNX → **动态量化 int8**（≈100–110 MB） | 比 fp32 缩小约 4 倍，CPU 可跑，无需 GPU |
| 分发方式 | **不随主包**，做成独立「精度增强包」`.zip`，用户**离线导入** | 与现有「附件随包 + 缺失清单」的思路一致，主包体积不增长 |
| 分平台 | x86_64 优先启用；ARM64 需先核对 onnxruntime 支持 py3.9 的版本档 | 与本仓库「分平台/分 Python 版本锁版本」的既有做法一致 |
| 降级策略 | 模型不存在 / 加载失败 → 自动回落三级流水线 | 任何情况下**不得阻塞纠错主流程** |
| 线程 | 走现有 `ui/workers.py` 的 `FnWorker` | 界面不卡死 |

> ⚠️ `onnxruntime` 新版要求 Python ≥ 3.11，而本项目 ARM64 CI 用 3.9 —— **必须在 `requirements.txt` 里分档锁版本**，并在 CI 增加断言。

---

## 6. 分阶段落地路径

| 阶段 | 内容 | 新增依赖 | 风险 | 收益 |
|------|------|---------|------|------|
| **Phase 1**（建议立即做） | ① 删除死条目 `恢复→恢复`；② `CONTEXT_RULES` 扩到 **30–50 条**、`NEGATIVE_CONTEXTS` 扩到 **10 条+**；③ 补一批公文高频精标对；④ 补对应单元测试 | **无** | 极低 | 高——直接抬精度天花板，当天可回归验证 |
| **Phase 2**（软性） | 从公开词典资源（如 houbb 的 1397 条对照、公开错别字表）**抽取去重**合并进 `CURATED_PAIRS` | 无 | 低 | 中——扩精标规模；**须标注出处与许可证** |
| **Phase 3**（可选·重） | ONNX 精度增强包 + 插件式加载器 + 设置项开关 + 增强包导入 UI | `onnxruntime` | 中 | 高——补"词表外错字"能力 |
| **Phase 4**（仅硬需求） | 评估语法纠错（CGEC），需对接 4B 级模型 | 重 | 高 | 视需求 |

---

## 7. 风险与红线

- **许可证可追溯**：党政机关项目必须能逐条说清「每个词库 / 每个模型」的来源与许可证。引入任何外部数据前先登记。
- **体积红线**：主包体积不得因纠错功能增长 —— 建议在 `scripts/smoke_dist.py` 增加**体积上限断言**。
- **误报红线**：任何新数据 / 新模型上线前，必须同时通过两道门禁 ——
  ① 精标集识别率 **100%**（现有 `test_jianku_100_curated_pairs`）；
  ② **公文体负样本 0 误报**（需新建一组"正确但含易混词"的负样本用例）。
- **零网络红线**：ONNX 模型必须**随增强包本地加载**，严禁任何形式的在线拉取。

---

## 8. 待你决策

1. 是否现在就推进 **Phase 1**（无依赖、可当天落地）？
2. 是否需要**语法纠错**？（直接决定要不要碰 4B 级模型，Phase 4）
3. ARM64 端能否接受「纠错精度略低于 x86_64」？（决定 L4 是否分平台启用）
