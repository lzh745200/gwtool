# 公文汇编助手：v1.6.0 增强方向分析与实施方案

> 本文面向产品负责人与维护者，回答：**v1.6.0 之后还该做什么**。
> 所有判断均来自对当前代码基线的**实测复现**，逐条附证据落点；不含凭印象的推测。
>
> **核查基线**：v1.6.0，`gwtool/` 20,610 行 / 76 个模块，测试 **1121 条**，`ruff check .` 全过。
> 双平台 CI（Windows x64 + 麒麟 ARM64）。

---

## 〇 本文的定位（先说清楚不做什么）

仓库已有四份方案文档，**本文不重复其中任何一条**：

| 文档 | 基线 | 状态 |
| --- | --- | --- |
| `纠错系统落地方案.md` | v1.5.1 | 引擎选型与量化路线 |
| `纠错引擎增强方案.md` | v1.5.1 | 四级流水线架构 |
| `纠错系统差距分析与增强实施计划.md` | v1.5.2 | D1–D9 缺陷清单 + P0–P3 |
| **`系统功能盘点与增强方案.md`** | **v1.5.4** | **14 项已全部落地为 v1.6.0** |

**前三份聚焦纠错引擎**；第四份盘点纠错之外的系统能力，其 14 项（收文台账 / 时限督办 / 归档保管 / XLSX 导出 / 批量移交 / 诊断日志 / 健康自检 / 诊断包 / 引文体检 / 数字一致性 / 文风检查 / 来源清单 / 性能基线 / 数据库自检）**已于 2026-09-16 全部实施完毕**。

**基线变了，所以本文重新盘点。** v1.6.0 一口气加了 2,222 行新代码、14 个新模块——**这些新能力本身从未经过"性能 / 容错 / 完整性 / 可用性"四个维度的检验**，本文正是补这一课。

### 与既有方案的关系

- 第四份方案 §6.5 如实记录了「**新增界面的人眼验收：未做**」——本文的 U 类正是接续这一条。
- 第四份方案 §四 的「明确不建议做」6 项（云同步 / 大模型改写 / 换架构 / 引大依赖 / 常驻进程 / 用户可编辑正则）**本文全部继承，不再重复论证**。
- 第四份方案 §6.4 已用性能基线否决「列表分页 / 虚拟列表」——**本文不再重提**。

---

## 一 现状盘点：v1.6.0 基线

### 1.1 规模与门禁

| 指标 | v1.5.4 | **v1.6.0** |
| --- | --- | --- |
| 产品代码 | 17,469 行 / 67 模块 | **20,610 行 / 76 模块** |
| 测试 | 799 | **1121**（60 个文件） |
| `ruff check .` | 通过 | 通过 |
| e2e 检查项 | 20 | 20 |
| **静默吞咽异常** | 62 处 | **79 处** |

### 1.2 一个必须先说清的底盘指标

**79 处「吞掉异常且不留痕」**（v1.5.4 时 62，一版增加 17 处）。这不是"坏味道"层面的问题——本项目**刻意**采用了"异常必须咽掉 + 静默降级"的设计（可选增强包、OCR、TTS 均如此，这是正确的）。真正的问题是**其中混着一批"该留痕却没留"的**：

| 分类 | 数量 | 例 |
| --- | --- | --- |
| **可保持**（合理的降级设计） | ~55 | 可选增强包缺失、日志自身失败、TTS 不可用 |
| **该补日志**（掩盖真问题） | ~16 | `exporter.py:76` 原文件读失败静默退化；`model.py:63` 坏 `blocks_json` 退成空结构 |
| **该改判定**（成功/失败口径错了） | ~4 | 见 S1 / D1 / F2 / F3 |

本文的 F 类与 S1 就是对这三类的处置。

---

## 二 可增强之处：20 项

按**缺口性质**分五类：**S 安全隐私** / **D 数据完整性** / **P 性能响应性** / **F 容错可诊断** / **U 可用性离线实操**。

每项给出：现状证据 / 目标 / 实现思路 / 涉及范围 / 验证方式 / 如何强化核心功能。

---

### S 类：安全与隐私（两条都是"承诺与实现不一致"）

#### S1 诊断包会把公文正文原样带出 —— `redact()` 护栏形同虚设 ★最高

| 项 | 内容 |
| --- | --- |
| **现状证据** | `redact()` 定义于 `logs.py:40`，**全仓库 0 处调用**（grep 只命中定义与文档串）。`diagpack.build()`（`diagpack.py:191-193`）把**整份原始日志**塞进 zip；`logs.py:134` 的 excepthook 落**完整 traceback**。而 UI 的 `main_window.py:438-439` 与 `preview_lines()`（`diagpack.py:185`）都向用户承诺"**不含任何公文正文**" |
| **实测复现** | 构造含正文标记的异常 → 写日志 → 生成诊断包 → 解压 `运行日志.txt`：**标记原样命中 2 次** |
| **测试为何没拦住** | `test_diagpack.py:102` 的 `test_pack_contains_no_document_text` 只把标记写进**数据库**、**从不写日志** —— 对**日志通道零覆盖**。20 个用例全过 |
| **缺口本质** | 党政机关场景下，"诊断包可以发给运维/厂商"这个前提**完全靠"不含正文"撑着**。现在它不成立，而用户看到的是"不含任何公文正文"——这是**承诺与实现相反** |
| **目标** | 任意异常路径下，日志与诊断包都不含正文，且**可自动断言** |
| **实现思路** | ① `logs.py` 加 `RedactingFilter`，在 QueueHandler **之前**对 `message`/`args` 做长度化（超长参数只留前 N 字 + 省略标记）；② excepthook 只保留**异常类型 + 末帧 `file:line`**，丢弃 args 渲染；③ 新增 `tests/test_logs_privacy.py`：注入含标记的日志 → **断言日志与诊断包均不含该标记**（把"编码纪律"变成断言）。`logs.py:8` 处自身失败的静默**保持不变**（那是对的） |
| **涉及范围** | `gwtool/logs.py`、`gwtool/core/diagpack.py`、`tests/test_diagpack.py`、新增 `tests/test_logs_privacy.py`。不改 DB schema、不动打包 |
| **优先级** | **P0**（价值极高 / 成本低） |
| **验证方式** | 新增隐私断言全绿；复跑泄漏探针命中数 **= 0**；`test_diagpack.py` 原 20 项仍全绿 |
| **如何强化核心功能** | 让"一键诊断包"在涉密场景下**真的敢发**——否则这个功能在最需要它的环境里不可用 |

#### S2 台账 CSV 导出存在公式注入

| 项 | 内容 |
| --- | --- |
| **现状证据** | `registry.py:115-127` 原样写值，不做转义 |
| **实测复现** | 标题写 `=cmd\|'/c calc'!A1`、备注写 `+1+1` → **原样落盘**（同样数据走 xlsx 因用 inlineStr 反而安全） |
| **目标** | 以 `= + - @` 起首的单元格值在 CSV 中被中和 |
| **实现思路** | 导出时对以这四个字符起首的值加前置 `'`（是 CSV/Excel 通行的防御写法，不改数据本身） |
| **涉及范围** | `core/registry.py`、`core/receive.py`（同源导出），不动 xlsx 通路 |
| **优先级** | **P2**（价值中 / 成本极低） |
| **验证方式** | 断言导出行首被转义；往返导入后原值不变 |
| **如何强化核心功能** | 台账导出的文件会被拿去别的机器打开，不中和等于把风险传出去 |

---

### D 类：数据完整性

#### D1 恢复校验「只查结构不查内容」→ **合法空库能静默顶掉真库** ★

| 项 | 内容 |
| --- | --- |
| **现状证据** | `backup.py:694-707` 只判 `CORE_TABLES` 是否存在；**报错文案却写着"疑似空库"**（说明作者意识到风险，但校验未覆盖） |
| **实测复现** | 真库 2 篇 → 恢复一个"4 张核心表都在、但**全空**"的**合法**库 → `ok=True`、**无任何告警** → 文档变 **0 篇** |
| **缺口本质** | 此前已修过"坏包/截断包"（三层校验：文件头 + `integrity_check` + 业务表齐全）。但**"合法但空的包"是另一类**：它通过了全部结构校验，内容却是空的 |
| **目标** | 换库前发现"内容骤减"并交用户二次确认，绝不静默替换 |
| **实现思路** | 换库**之前**只读比较包内与当前库的 `count(documents)`（及可选 `count(receive_register)`）；若包内为 0 而当前非 0，或降幅超阈值（如 >50% 且绝对值 >N），写入 `RestoreReport` 的告警字段，由 UI 弹二次确认 |
| **涉及范围** | `core/backup.py`、`ui/feature_dialogs.py`。不改表结构 |
| **优先级** | **P0**（价值极高 / 成本低） |
| **验证方式** | 新增"用合法空库恢复"用例，**必须出现告警**；现有 `test_backup_compat.py` 与恢复类用例不得回归 |
| **如何强化核心功能** | 把灾难恢复**最后一道**静默数据丢失路径堵死 |

#### D2 健康自检看不见「数据在却搜不到」

| 项 | 内容 |
| --- | --- |
| **现状证据** | `dbhealth.py:74-88` 只跑页级 `quick_check` |
| **实测复现** | 删掉某文档的 FTS 行后：`quick_check = (True, 'ok')` —— 但该文档**检索从 1 变 0**。孤儿附件/快照同样报 ok |
| **目标** | 自检覆盖"结构健康"之外的**交叉一致性**：索引与内容、附件/快照与文档引用 |
| **实现思路** | 自检加 3 条**只读**交叉查询：① FTS 行数 vs `documents` 行数；② 附件 `doc_id` 悬空；③ 快照 `doc_id` 悬空。发现失配时给出**一键修复**入口（复用既有 `dao.rebuild_fts` / `attachments.sweep_orphans`） |
| **涉及范围** | `core/dbhealth.py`（+ `ui/main_window.py` 展示）。不改 schema |
| **优先级** | **P0**（价值高 / 成本中低） |
| **验证方式** | 注入 FTS 失配后自检**必须非 ok**；修复后回到 ok |
| **如何强化核心功能** | "检索搜不到"是用户最困惑的故障之一，现在能被自检发现并自愈 |

#### D3 e2e 的「备份」检查只验了文件存在 —— 全绿不等于备份能用

| 项 | 内容 |
| --- | --- |
| **现状证据** | `e2e_check.py:186-189` 第 20 项只有 `Path(bz).exists()` |
| **实测复现** | 注入"备份=空库"缺陷后：pytest **11 条报错**，但 **e2e 该项仍然通过**，恢复后文档 2→0 |
| **缺口本质** | e2e 是发布前的最后一道人工可读的门禁，**它现在会对"备份不可用"给出绿灯** |
| **目标** | e2e 覆盖「备份 → 恢复 → 内容一致」的**完整往返** |
| **实现思路** | 在既有备份步骤之后补：恢复该备份 → 断言文档数与标题集合与恢复前一致。**必须先 `paths._override` 到临时目录**，绝不碰用户真实数据 |
| **涉及范围** | `scripts/e2e_check.py`（约 15 行） |
| **优先级** | **P0**（价值高 / 成本极低） |
| **验证方式** | 注入缺陷后 e2e **必须变红**（这是本项的验收标准） |
| **如何强化核心功能** | 把"备份可用性"纳入发布门禁，而不是靠人记得去试 |

#### D4 xlsx 工作表名含 `"` → 工作簿 XML 非法 → **Excel 报"文件已损坏"**

| 项 | 内容 |
| --- | --- |
| **现状证据** | `xlsx.py:42` 的非法字符集**漏了 `"`**；`:243` 用 `name="{_clean(name)}"` 拼属性，而 `escape()` **不转义引号** |
| **实测复现** | `export_report_xlsx(..., source_name='关于"某某"的通知.docx')` → `xl/workbook.xml` **not well-formed**。该名取自**用户文件名**（`feature_dialogs.py:171`），而**麒麟/Linux 文件名含 `"` 合法**，所以真实可达 |
| **目标** | 任意用户文件名都能导出为 Excel 可正常打开的工作簿 |
| **实现思路** | `_sheet_name` 补清洗 `"` 与控制字符；XML 属性值改用 `quoteattr`（而非手写双引号 + 不转义的 `escape`）。顺带核 `:243` 之外所有属性拼接点 |
| **涉及范围** | `core/xlsx.py`（影响发文/收文台账、归档清单、体检清单全部导出） |
| **优先级** | **P0**（价值高 / 成本极低） |
| **验证方式** | `test_xlsx.py` 增引号名用例，断言**全部件可被 XML 解析器解析** |
| **如何强化核心功能** | 导出是"材料出得去"的最后一环，产出损坏文件等于白做 |

#### D5 迁移失败被吞 + 版本号无条件顶格

| 项 | 内容 |
| --- | --- |
| **现状证据** | `schema.py:281-289` 非 duplicate 类迁移错误**只 warning 后继续**；`:294` **无条件**写 `user_version=4`。另：旧版程序打开 v4 库会把版本**改写回 3**（`connection.py:114`） |
| **实测复现** | 注入必失败语句 → **不抛错、版本仍 = 4、重启不重试**。即：库处于"半迁移 + 版本号说谎"状态 |
| **目标** | 迁移**全成功才写版本号**；版本不匹配时先兜底备份并明确提示，绝不静默改写 |
| **实现思路** | ① 迁移链入**单事务**，全部成功才写 `user_version`；② 打开时若 `ver > SCHEMA_VERSION`（旧程序遇到新库）→ 先做一次兜底备份 + 明确提示"此库由更新版本创建"，**不再改写版本号** |
| **涉及范围** | `db/schema.py`、`db/connection.py`。**触碰迁移链，须单独提交** |
| **优先级** | **P1**（价值高 / 成本中） |
| **验证方式** | 断言迁移失败时**抛错且版本不变**；断言新库被旧程序打开时版本不被改写 |
| **如何强化核心功能** | 防止"升级到一半"的库被当成正常库继续用 |

#### D6 移交包只导出无导入，`sha256` 只写不校验

| 项 | 内容 |
| --- | --- |
| **现状证据** | `core/exporter.py` **无 import/restore 函数**；`read_manifest` **无调用方**；`documents[].sha256` **从未被核验** |
| **缺口本质** | 对外宣称的"**往返无损**"**在代码上根本无法达成**——没有校验，也没有回程 |
| **目标** | 至少先做到"**能校验**"：验证移交包完整、未被篡改、条目齐全 |
| **实现思路** | 新增只读 `verify_package()`：逐条校验条目存在 + `sha256` 匹配 + manifest 计数一致；UI 在"批量导出与移交包"旁提供「**校验移交包**」入口。**导入回程可后置**（属更大工程），但校验必须先有 |
| **涉及范围** | `core/exporter.py`（+ `ui/main_window.py`） |
| **优先级** | **P2**（价值中 / 成本中） |
| **验证方式** | 篡改包内任意字节后 `verify` **必须失败**；未篡改包必须通过 |
| **如何强化核心功能** | 让"移交包"从"只能信任"变成"可验证" |

---

### P 类：性能与响应性

#### P1 把 v1.6.0 新增的重型操作移出主线程（+ 维护失败必须可见）

> **合并说明**：性能审计的「`rebuild_fts` 主线程直调」与容错审计的「维护操作零失败反馈」**指向同一函数 `feature_dialogs.py::_rebuild_fts`**，故合并为本项。

| 项 | 内容 |
| --- | --- |
| **现状证据** | `feature_dialogs.py:1264` **主线程直调** `dao.rebuild_fts()`，3000 篇实测 **2.4–3.4 秒**（其中 jieba 分词正文 2.0s，SQL 插入 <0.3s）；`main_window.py:388` `exporter.build` 主线程 **1.5–2.1 秒**；`:433` `diagpack.build` 276ms；`:445/:457` `quick_check` 119ms / `maintenance`（内含 rebuild_fts） |
| **附带缺陷** | `feature_dialogs.py:1260-1266` 的 `_rebuild_fts` **无 try/except**，直接取 `counts['documents']` —— 失败时点按钮**零提示**（对照同文件 `_sweep_attachments`(1274) 有 try/warn）。这正是历史"维护成功但索引 0 条"的同型 |
| **目标** | 单次主线程占用 **< 100ms**；>0.5s 的操作显示进度且**可取消**；失败必须可见 |
| **实现思路** | 复用**已存在**的 `ui/workers.py::FnWorker`（`main_window.py:347/516` 已是此模式），把上述调用移入 worker 并用 signal 报进度；给 `rebuild_fts` 加 `progress_cb` 回调；`_rebuild_fts` 包 try/except + 失败 warn；FTS 重建结果为 0 条时**也显式提示**（当前仅 `fts_rows>=0` 即静默） |
| **涉及范围** | `ui/feature_dialogs.py`、`ui/main_window.py`、`core/dbhealth.py`、`db/dao.py`。不改 schema、不动打包 |
| **优先级** | **P1**（价值高 / 成本低-中） |
| **验证方式** | 3000 篇点「重建索引」时窗口**仍可交互/可取消**；断言操作在 QThread 内完成；断言失败路径有提示 |
| **如何强化核心功能** | 消除最长的一次 UI 冻结，让万篇级"索引自愈 / 移交导出"仍可用 |

#### P2 `exporter.build` 的 O(n²) 重名检查 —— **一行改动，占 54% 耗时**

| 项 | 内容 |
| --- | --- |
| **现状证据** | `exporter.py:137`：`any(e["path"] == name for e in entries)` —— 每篇都对已收集列表线性扫一遍 |
| **实测数据** | cProfile：`any` + genexpr 占 **1.6s / 3.0s（54%）**；隔离实测 3000 篇该检查 **327ms**，改用 `set` 后 **2ms** |
| **目标** | `exporter.build(3000)` 由 ~1.5s 降到 **<0.9s** |
| **实现思路** | 改 `seen: set[str]`；顺带把 `_document_payload` 的 `exists() and is_file()` 合并为**一次 stat**（cProfile 显示 `nt.stat` 占 0.5s） |
| **涉及范围** | `core/exporter.py`（**函数内改动，签名不变**） |
| **优先级** | **P0**（价值高 / 成本极低） |
| **验证方式** | 同批前后对比；断言重名文档仍正确追加 `_id` 后缀 |
| **如何强化核心功能** | 移交包导出的固定成本**减半** |

#### P3 批量归档逐行提交 → 单事务

| 项 | 内容 |
| --- | --- |
| **现状证据** | `archive.py:83` 逐行 `dao.update_receive`（内部 per-row commit） |
| **实测数据** | 1000 行 **197ms**、2000 行 **332ms**；同操作单事务 **19ms / 41ms**（**8–10×**） |
| **目标** | `batch_archive(2000)` **< 50ms** |
| **实现思路** | `batch_archive` 内用单事务（集中 commit / `executemany`）；顺检 `core/batch.py` 的批量纠错是否有同类逐行 commit |
| **涉及范围** | `core/archive.py`（+ 可选 `db/dao.py` 批量函数）。**不改表结构** |
| **优先级** | **P1**（价值中 / 成本低） |
| **验证方式** | 探针对比前后耗时；断言 `archived`/`skipped` 计数一致 |
| **如何强化核心功能** | 一次归档数百件的卡顿消除 |

#### P4 `rebuild_fts` 增量化

| 项 | 内容 |
| --- | --- |
| **现状证据** | 每次对全库重新 jieba 分词（tokenize 占 2.0s / 2.3s）；而 `documents` 表**已有 `text_hash` 列**可复用 |
| **目标** | 无内容变更时第二次重建 **< 0.2s** |
| **实现思路** | 按 `text_hash` 比对照，仅对**变更/缺失行**重分词（DELETE + INSERT 影子表） |
| **涉及范围** | `db/dao.py::rebuild_fts`。不改 schema |
| **优先级** | **P2**（价值中 / 成本中）。**P1 落地后可降为储备项** |
| **验证方式** | 连续两次重建，第二次 < 0.2s 且条数不变 |
| **如何强化核心功能** | 万篇库的索引自愈从数十秒降到秒级 |

---

### F 类：容错与可诊断

#### F1 「安全网失败仍覆盖原文」—— 快照降级必须可见

| 项 | 内容 |
| --- | --- |
| **现状证据** | `batch.py:431-434` 快照失败 `except Exception: pass`，`:438` **仍** `update_document_content` 覆盖原文；`feature_dialogs.py:271-274` 同构 |
| **实测复现** | 令 `add_snapshot` 抛 `OSError` → 正文被改（ABC→XYZ）、`failures=[]`、**无任何日志**。用户以为能回滚，**实际不能** |
| **缺口本质** | 快照存在的意义就是"改错了能回退"。快照失败还继续改，等于**把安全网撤了再动手**，而且不告诉用户 |
| **目标** | 快照失败必须出现在 `failures` 列表并写日志 |
| **实现思路** | 两处共用 helper：留 `logs.warning` + 向 `result.failures` 追加「(标题, 正文已改，但回滚快照未保存：…)」 |
| **涉及范围** | `core/batch.py`、`ui/feature_dialogs.py` |
| **优先级** | **P1**（价值高 / 成本低） |
| **验证方式** | 复跑快照失败探针，断言 `failures` 非空且日志有记录 |
| **如何强化核心功能** | 保住"批量改写可回退"这一承诺 |

#### F2 备份列表 / 恢复的静默降级 → 显式呈现

| 项 | 内容 |
| --- | --- |
| **现状证据** | `backup.py:499` `except Exception: continue`；`backup.py:587` 附件恢复失败静默，`RestoreReport.ok` **仍为 True** |
| **实测复现** | 磁盘放 2 个 `.zip`，`list_backups` **只返回 1 个** —— 损坏的备份**从列表里凭空消失**，无任何解释 |
| **目标** | 损坏备份**被列出并标记**；附件恢复失败**计入报告** |
| **实现思路** | `list_backups` 捕获后 append `{"file", "corrupt": True, "error"}`；`restore_backup_detailed` 把 581/587 的异常并入 `RestoreReport` 新字段并在对话框展示 |
| **涉及范围** | `core/backup.py`、`ui/feature_dialogs.py`、`tests/test_safety_probe.py` |
| **优先级** | **P1**（价值高 / 成本低） |
| **验证方式** | 放一个损坏包，断言它**出现在列表里且被标记**；断言恢复报告含附件失败项 |
| **如何强化核心功能** | 备份列表是用户唯一的"我有哪些备份"视图，**看不见 = 不存在** |

#### F3 设置类写失败无痕

| 项 | 内容 |
| --- | --- |
| **现状证据** | `csc_gec.py:170`、`reminder.py:39` |
| **实测复现** | 用户改开关**"成功"**（无任何提示），**重启后丢失** |
| **目标** | 设置写失败必须提示 |
| **实现思路** | 两处补 `logs.warning` + 调用方提示；顺检其他 `set_setting` 静默点 |
| **涉及范围** | `core/csc_gec.py`、`core/reminder.py`（+ 调用方） |
| **优先级** | **P2**（价值中 / 成本极低） |
| **验证方式** | 令写库失败，断言出现提示 |
| **如何强化核心功能** | 消除"设了不生效"的困惑源 |

---

### U 类：可用性与离线实操

> 这一类直接接续 v1.5.4 方案 §6.5 那句「**新增界面的人眼验收：未做**」。以下每条都来自**实际走查**（离屏渲染 + 逐控件核对），不是按程序员审美提的需求。

#### U1 批量导入的失败原因被吞掉，用户拿不到失败清单

| 项 | 内容 |
| --- | --- |
| **现状证据** | `workers.py:98-100` —— 解析失败仅 `skip += 1; continue`，`r.error` **直接丢弃**；`import_dialog.py:193` 只报"成功 N 篇；重复/失败/扫描版跳过 M 篇" |
| **值得注意** | `importer.py:29-31` 与 `:66` **精心写好的中文 OCR / 缺 Tesseract 指引，在批量路径里永远看不到** |
| **目标** | 导 200 份后，用户知道"**哪几份没进、为什么、接下来做什么**" |
| **实现思路** | `ImportWorker` 收集 `(文件名, 原因)` 列表经新 signal 回传；`_done` 展示前 8 条 + 提供「**导出失败清单**(TXT/CSV)」按钮；原因文案**复用 `importer` 已有中文串** |
| **涉及范围** | `ui/workers.py`、`ui/import_dialog.py`（约 60 行） |
| **优先级** | **P0**（价值高 / 成本低） |
| **验证方式** | 5 份损坏/不支持文件批量导入，断言逐条清单出现、且文案**不含异常类名** |
| **如何强化核心功能** | 材料收集是"进料口"，**进料不可见则后面全白做** |

#### U2 错误文案剥离 Python 异常类名与英文原文

| 项 | 内容 |
| --- | --- |
| **现状证据** | 实测 `parse_any` 返回值：损坏 docx → `PackageNotFoundError: Package not found at 'C:\...\损坏文件.docx'`（`importer.py:90`）；文件不存在 → `FileNotFoundError: no such file: '...'`；空文件/坏图 → 仅"未提取到文字"，**无下一步**。外部双击打开路径会把它**原样弹给用户**（`main_window.py:523-525`） |
| **缺口本质** | 目标用户是文书岗人员，不是技术人员；而**离线内网环境没有客服**，**提示就是唯一的售后** |
| **目标** | 显示「该文件可能已损坏或不是有效的 Word 文档，请用 Word/WPS 打开后另存为 .docx 再试」这类**可执行**文案 |
| **实现思路** | `importer.py` 加 `_friendly_error(exc, path)`：`PackageNotFoundError` / `FileNotFoundError` / `PermissionError` / 磁盘满 → 中文可执行文案；**原始文本只进日志** |
| **涉及范围** | `core/importer.py` + 两条入口文案（`main_window.py`、`import_dialog.py`） |
| **优先级** | **P0**（价值高 / 成本低） |
| **验证方式** | 断言 4 类错误文案**均不含 `Error` / 英文类名** |
| **如何强化核心功能** | 把"报错"从"用户看不懂"变成"用户知道下一步" |

#### U3 空库首启没有任何引导

| 项 | 内容 |
| --- | --- |
| **现状证据** | 走查：空库首启三栏全空；`library_panel.py:157-172` 只有 `setItem` + 计数标签，**无引导**；而编辑器占位写着"**双击左侧材料打开编辑**"——**左侧是空的** |
| **附带实测** | 缺公文字体的机器上，`MainWindow.__init__ → _first_run_checks`（`main_window.py:813-820`）**当场弹模态警告** —— 离屏探针在 `MainWindow()` 一行即被卡死，证明它是**启动路径上的阻塞弹窗**：用户什么都还没做先被技术性警告拦一次 |
| **目标** | 首启一眼看到「① 导入材料 ② 新建公文 ③ 一键汇编」 |
| **实现思路** | `library_panel._reload_docs` 在 docs 为空且无检索词时插入一条**不可选中的引导项**（含按钮/快捷键提示）；编辑器占位同步改为"还没有材料，先点「导入材料」"；首启字体警告改为**非模态提示条**（不阻塞启动） |
| **涉及范围** | `ui/library_panel.py`（约 30 行）、`ui/editor_panel.py`、`ui/main_window.py` |
| **优先级** | **P1**（价值中高 / 成本低） |
| **验证方式** | 离屏渲染空库截图，断言存在引导文本；断言构造 `MainWindow` **不被模态框阻塞** |
| **如何强化核心功能** | 直接降低"装完不会用 → 弃用"的比例 |

#### U4 「设置 — 系统与安全」无滚动区，小屏底部被裁

| 项 | 内容 |
| --- | --- |
| **现状证据** | 实测 `sizeHint`：内容需 **982×798**，却 `resize(640,720)` 且 `hasScrollArea=False`（`feature_dialogs.py:869-871`）。而 `ReceiveForm` 因"24 字段小屏超屏"**专门加了 `QScrollArea`**（`receive_dialog.py:105-110`）—— **同一问题只修了一处** |
| **后果** | 1366×768 笔记本上，底部按钮（清理附件 / 关闭）**不可达** |
| **目标** | 任何 768p 笔记本都能滚到全部设置项 |
| **实现思路** | `SecurityDialog` 内容套 `QScrollArea`（**照抄 `ReceiveForm` 的写法**）；顺带核 `AnyDocCorrectDialog`(720) / `CompareDialog`(640) |
| **涉及范围** | `ui/feature_dialogs.py`（约 10 行） |
| **优先级** | **P1**（价值中 / 成本低） |
| **验证方式** | 构造后断言 `QScrollArea` 存在且高度受屏限制 |
| **如何强化核心功能** | 设置是安全运维的**唯一入口**，够不到等于没有 |

#### U5 产品内零帮助，tooltip 覆盖严重不足

| 项 | 内容 |
| --- | --- |
| **现状证据** | 全仓库唯一"帮助"菜单只有「生成诊断包 / 关于」（`main_window.py:257-262`）；工具栏 **15 个动作仅 3 个有 tooltip**（`main_window.py:173/177/181`），**12 个没有**；整个 `ui/` 目录仅约 30 处 `setToolTip` |
| **缺口本质** | 内网环境**没有在线文档可查** → tooltip 是**唯一的说明书** |
| **目标** | 不懂电脑的文书员鼠标停一下就知道"排版微调""格式体检"是什么 |
| **实现思路** | 为 12 个无 tooltip 的工具栏动作、汇编向导三步、收文表单关键字段（"拟办意见" vs "领导批示"、"保管期限"）各补一句静态说明。**零依赖、零网络** |
| **涉及范围** | `ui/main_window.py`、`ui/compile_wizard.py`、`ui/receive_dialog.py` |
| **优先级** | **P1**（价值中 / 成本低） |
| **验证方式** | 断言关键动作 `toolTip()` 非空 |
| **如何强化核心功能** | 让功能"能被发现"，否则做了也没人用 |

#### U6 汇编向导选材料无筛选/全选，且默认全勾

| 项 | 内容 |
| --- | --- |
| **现状证据** | `compile_wizard.py:41-49`：所有材料堆进一个**无筛选、无分组**的 `QListWidget`，**无全选/反选**，且**默认全勾** |
| **缺口本质** | 材料多时**默认全勾反而危险**（容易误把无关材料编进去） |
| **目标** | 可按分类/标签筛选，并有全选/全不选/反选 |
| **实现思路** | 加分类/标签筛选 + 三个批量勾选动作 |
| **涉及范围** | `ui/compile_wizard.py` |
| **优先级** | **P2**（价值中 / 成本中） |
| **验证方式** | 断言筛选后列表项数正确、全选/反选生效 |
| **如何强化核心功能** | 汇编是本产品的核心动作，选材效率直接决定它好不好用 |

---

## 三 优先级排序（价值 × 成本）

### 3.1 汇总表

| 优先级 | 编号 | 项目 | 价值 | 成本 | 依赖 | 一句话理由 |
| --- | --- | --- | --- | --- | --- | --- |
| **P0** | S1 | 诊断包泄露正文 | **极高** | 低 | 无 | 承诺与实现相反；涉密场景下该功能当前**不可用** |
| **P0** | D1 | 合法空库静默顶掉真库 | **极高** | 低 | 无 | 灾难恢复最后一道静默数据丢失路径 |
| **P0** | P2 | exporter O(n²) | 高 | **极低** | 无 | 一行改动换 54% 耗时 |
| **P0** | D4 | xlsx 工作表名引号 | 高 | **极低** | 无 | 产出"Excel 打不开"的文件 |
| **P0** | D3 | e2e 备份往返断言 | 高 | **极低** | 无 | 门禁对"备份不可用"给绿灯 |
| **P0** | U1 | 批量导入失败原因被吞 | 高 | 低 | 无 | 进料口不可见，后面全白做 |
| **P0** | U2 | 错误文案暴露异常类名 | 高 | 低 | 无 | 离线无客服，提示就是唯一售后 |
| **P0** | D2 | 健康自检盲区 | 高 | 中低 | 无 | "数据在却搜不到"报健康 |
| **P1** | P1 | 重型操作移出主线程(+失败可见) | 高 | 低-中 | 无 | 消除最长一次 UI 冻结 |
| **P1** | F1 | 快照失败仍覆盖原文 | 高 | 低 | 无 | 安全网失败却悄悄动手 |
| **P1** | F2 | 备份列表/恢复静默降级 | 高 | 低 | 无 | 损坏备份凭空消失 |
| **P1** | D5 | 迁移失败+版本顶格 | 高 | 中 | 无 | "半迁移库"被当正常库用 |
| **P1** | P3 | 批量归档单事务 | 中 | 低 | 无 | 8–10× 提速 |
| **P1** | U3 | 空库首启无引导 | 中高 | 低 | 无 | 降低"装完不会用" |
| **P1** | U4 | 设置对话框无滚动区 | 中 | **极低** | 无 | 小屏底部按钮不可达 |
| **P1** | U5 | tooltip 覆盖不足 | 中 | 低 | 无 | 内网无在线文档，tooltip 是唯一说明书 |
| **P2** | D6 | 移交包无校验 | 中 | 中 | 无 | "往返无损"无法达成 |
| **P2** | S2 | CSV 公式注入 | 中 | **极低** | 无 | 把风险随文件传出去 |
| **P2** | P4 | rebuild_fts 增量 | 中 | 中 | P1 | P1 落地后可降为储备 |
| **P2** | F3 | 设置写失败无痕 | 中 | **极低** | 无 | 消除"设了不生效" |
| **P2** | U6 | 汇编选材无筛选 | 中 | 中 | 无 | 默认全勾反而危险 |

### 3.2 建议的执行节奏

**第 1 批（全部"极低/低成本"，可独立交付、独立回退）**
> S1 → D3 → P2 → D4 → S2 → U4 → F3

选择理由：这 7 项**改动都在函数内、不动 schema、不碰打包**，且 **P0 中 3 项成本极低**（D3/P2/D4）。
**S1 排第一**是因为它是"承诺与实现不一致"，性质最严重。

**第 2 批（数据安全与核心体验）**
> D1 → F1 → F2 → U1 → U2 → D2

选择理由：D1/F1/F2 同属"静默失败掩盖真问题"，**同一批修完便于一次性补齐断言**；U1/U2 是用户每天都会碰到的进料口与报错。

**第 3 批（响应性与迁移）**
> P1 → P3 → D5

**D5 触碰迁移链，必须单独提交**，便于定位。

**第 4 批（体验补强与储备）**
> U3 → U5 → P4 → D6 → U6

---

## 四 明确不建议做的事

**继承 v1.5.4 方案的 6 项**（云同步 / 大模型改写 / 换架构 / 引大依赖 / 常驻进程 / 用户可编辑正则）——不再重复论证。

**本轮新增的 5 项"不做"**（均有实测依据）：

| 候选 | 为什么不建议 |
| --- | --- |
| 给 `receive_register` 加索引 / 改查询 | 实测 3000 行列表 **73ms**、关键字 LIKE 4.8ms、`max_reg_no_serial` 2.8ms —— 现有 `idx_receive_no/date/org/due` **已足够** |
| XLSX 改流式 / 拆分临时文件 | 实测 3000 行×25 列 **272ms**、峰值 19MB 可接受；且"**不用临时文件**"是刻意的**原子性设计** |
| WAL / checkpoint 调优 | 500 次小写后 WAL 7.4MB，checkpoint 后归 0；默认 1000 页自动 checkpoint，`maintenance` 已 TRUNCATE |
| 启动期预加载 jieba | `jieba.initialize()` 880ms 但是**惰性**的，且新模块**全部惰性 import**（exporter 127ms / xlsx 60ms 仅在使用时付）——预加载是把成本提前到用户不需要时付 |
| 收文台账表格虚拟化 | 3000 行填充 **71ms**，与 §6.4 已否决的分页优化**同类** |
| **改 `synchronous=NORMAL`** | 掉电可丢最近已提交事务。这是**性能/持久性权衡**，**不该擅自升等级拖慢写入**（可在自检提示中说明，不改默认） |
| **把 `CORE_TABLES` 扩成"全表"** | 会**重演**"新增一张表 → 用户所有旧备份不可恢复"的陷阱；`test_backup_compat.py` 已固化该约束 |
| 静默吞咽一刀切改 `raise` | 79 处里约 55 处是**正确的降级设计**（可选增强包、日志自身失败、TTS 不可用）。全抛违反"不引新障碍" |
| 日志改同步 `fsync` | 违反诊断日志"非阻塞"的设计前提 |
| 给日志加"正文哈希" | 对正文可**字典攻击**，无收益 |
| 在线文档 / 教程视频 / FAQ 网页 | 落在"**内网零网络**"红线之外 —— 用户**物理上打不开** |
| 为收文/发文台账加"回收站" | 现有二次确认已写明"此操作不可撤销"；成本高于收益 |

---

## 五 验证方式汇总

### 5.1 每项的验收判据（摘要）

| 项 | 关键判据 |
| --- | --- |
| S1 | 注入含标记的日志 → **日志与诊断包均不含标记** |
| D1 | 用"合法空库"恢复 → **必须出现告警** |
| D2 | 注入 FTS 失配 → 自检**必须非 ok**；修复后回 ok |
| D3 | 注入"备份=空库" → **e2e 必须变红** |
| D4 | 引号文件名导出 → **全部件可被 XML 解析** |
| P2 | 前后耗时对比；重名仍正确加 `_id` 后缀 |
| P1 | 3000 篇操作期间**窗口可交互/可取消**；断言在 QThread 内 |
| F1 | 快照失败 → `failures` **非空**且日志有记录 |
| F2 | 放损坏包 → **出现在列表且被标记** |
| U1 | 5 份坏文件 → **逐条清单**出现，文案不含异常类名 |
| U2 | 4 类错误文案**均不含 `Error`/英文类名** |
| D5 | 迁移失败 → **抛错且版本不变** |

### 5.2 三条门禁（每批交付前必跑）

```bash
export PATH="/c/Windows/System32:/usr/bin:/bin:$PATH"
cd C:/gwtool
PYTHONPATH= C:/gwtool/.venv/Scripts/python.exe -m pytest tests/ -q -p no:cacheprovider
C:/gwtool/.venv/Scripts/ruff.exe check .
PYTHONPATH= C:/gwtool/.venv/Scripts/python.exe scripts/e2e_check.py
```

### 5.3 必守的工程纪律

- **e2e 与任何破坏性探针必须先 `paths._override` 到临时目录**，绝不碰用户真实数据资产
- **新回归测试必须先在旧实现上确认失败（红 → 绿）**，避免写出恒过的断言
- **触碰 DB schema 的改动单独提交**（D5），便于定位与回退
- **临时脚本/日志写到仓库外**（写进仓库会让 `test_no_hardcoded_build_machine_paths` 全库扫到本机构建路径而误报）

---

## 附 核查命令（可复核）

```bash
# 1) redact() 零调用（应只命中定义与文档串）
grep -rn "redact" gwtool/ --include=*.py

# 2) 静默吞咽点总数（实测 79）
python -c "
import pathlib,re
t=0
for p in pathlib.Path('gwtool').rglob('*.py'):
    if '__pycache__' in str(p): continue
    t+=len(re.findall(r'except[^\n]*:\s*\n\s*(pass|continue)', p.read_text(encoding='utf-8')))
print('静默吞咽点:', t)"

# 3) e2e 备份那一步是否只断言存在（应只见 exists）
grep -n "备份" -A 4 scripts/e2e_check.py

# 4) exporter 的 O(n²) 检查（应命中 any(...for e in entries)）
grep -n "any(e\[" gwtool/core/exporter.py

# 5) xlsx 属性拼接点（应见 name=\"{...}\" 与 escape 不转义引号）
grep -n 'name="{_' gwtool/core/xlsx.py

# 6) 工具栏 15 个动作中有几个有 tooltip
grep -c "setToolTip" gwtool/ui/main_window.py

# 7) 设置对话框是否套了滚动区（应为 False）
grep -n "hasScrollArea" gwtool/ui/feature_dialogs.py
```
