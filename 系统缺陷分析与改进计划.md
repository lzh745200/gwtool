# 公文汇编助手（gwtool）全面缺陷分析与改进计划

> 基线：v1.6.0（`gwtool/` 76 模块 / 20,610 行；测试 1127 passed / 3 skipped）
> 审计方式：三路并行只读走查（UI 可用性 / 稳定性并发 / 兼容性打包）+ 对既有两份方案文档的落地复核
> 全部结论均附 `文件:行号` 证据，不含凭印象的推测

## 交付决定（已确认）

| 决定 | 取值 |
| --- | --- |
| **实施范围** | **全部 30 项一次性交付**（既有文档未落地 18 项 + 本次新增 12 项） |
| **D5 迁移链** | **按计划改** —— 迁移全成功才写 `user_version`；版本不匹配时兜底备份 + 明确提示，不再静默改写 |
| **提交策略** | D5 触碰迁移链，**单独成一次提交**（即便一次性交付，也保留独立回退点） |
| **交付形态** | 代码改动 + 3 条门禁全绿 + 分析报告归档；**不改版本号**（沿用 v1.6.0） |

选"全做"意味着改动面覆盖 `gwtool/` 下约 20 个模块 + 3 个新增文件 + CI 配置。
为避免"一次性交付"变成"一次性踩坑"，下节给出**强制分批提交**的执行编排：
**功能一次做全，但按 6 个批次分 6 次提交**，每批独立跑门禁，出问题可精确定位。

---

## 〇 先说清楚：哪些已经做过了

仓库已有 5 份方案文档。**本计划不重复其中任何一条**：

| 文档 | 状态 |
| --- | --- |
| `纠错系统落地方案.md` / `纠错引擎增强方案.md` / `纠错系统差距分析与增强实施计划.md` | 纠错引擎，已落地 |
| `系统功能盘点与增强方案.md` | 14 项已于 2026-09-16 全部落地为 v1.6.0 |
| **`v1.6.0增强方向分析与实施方案.md`** | 20 项（S1–S2 / D1–D6 / P1–P4 / F1–F3 / U1–U6）**已实施 8 项** |
| **`open-code-review审查报告.md`** | 12 处静默吞咽缺陷**已全部修复** |

**已复核确认落地的**（本次实测，非文档自述）：

```
✓ S1  日志脱敏 —— redact() 已接入 RedactingFilter（logs.py:79/95/109）+ diagpack 双通道
                    现调用点：logs.py:79,95,109,220 / diagpack.py:91,169（原为 0 处）
✓ 12 处静默吞咽修复 —— 静默点由 79 降至 67
```

**仍未落地的**（逐条实测确认）：S2、D1–D6、P1–P4、F1–F3、U1–U6 —— 共 18 项。
本次审计**新增 12 项**既有文档未覆盖的缺陷（下节 N 类）。

---

## 一 本次新增发现（12 项）

### N1 主窗口初始尺寸超出目标屏幕 —— 启动即"部分不可见" ★

| 项 | 内容 |
| --- | --- |
| **证据** | `ui/main_window.py:70` `self.resize(1360, 820)`；工具栏 `ToolButtonTextUnderIcon` 横向占位大，宽度已紧贴 1366 屏 |
| **后果** | 目标环境是 1366×768 办公笔记本。可用高度 ≈ 730px（扣任务栏+标题栏），**初始高度 820 直接超出 90px** —— 底部状态栏与编辑器底边首屏即被裁掉。**且这是每个用户的第一次启动印象** |
| **改法** | 初始尺寸改为"不超过可用屏幕的 90%"：用 `QGuiApplication.primaryScreen().availableGeometry()` 夹取；并设 `setMinimumSize(1024, 640)` 保证不会缩到布局破碎 |
| **涉及** | `ui/main_window.py`（约 6 行） |
| **优先级** | **P0**（每个用户第一眼就碰到） |
| **验证** | 构造后断言 `height() <= availableGeometry().height()` |

### N2 「设置 — 系统与安全」无滚动区，小屏底部按钮不可达 ★

| 项 | 内容 |
| --- | --- |
| **证据** | `ui/feature_dialogs.py:879` `SecurityDialog.resize(640, 720)`，**全类无 `QScrollArea`**（`grep hasScrollArea` 该文件 0 命中）。422 行控件堆在单个 VBox，"关闭"按钮在 `:1072-1074` 最底部 |
| **对照** | `ui/receive_dialog.py:105-110` 因"24 字段小屏超屏"**专门加了滚动区并写了注释** —— 同一问题只修了一处 |
| **后果** | 720 + 标题栏 ≈ 750px > 730px 可用高度 → **1366×768 上"清理附件""关闭"按钮够不到**。设置是安全运维的唯一入口 |
| **改法** | 照抄 `ReceiveForm` 的 `QScrollArea` 写法；`resize` 高度改为 `min(720, 可用高-80)` |
| **顺检** | `correct_dialog.py:40` `resize(1080,720)` 同型；`main_window.py` 已确认；`receive_dialog`/`registry_dialog`/`template_editor`/`compare_dialog` 均在安全区 |
| **涉及** | `ui/feature_dialogs.py`（约 12 行）、`ui/correct_dialog.py` |
| **优先级** | **P0**（价值中 / 成本极低） |
| **验证** | 断言 `QScrollArea` 存在且 `height() <= 可用高` |

### N3 主线程同步执行重型操作 —— 最长一次 UI 冻结

| 项 | 内容 |
| --- | --- |
| **证据** | 全部在 `clicked` 槽内、无 worker 包装：`main_window.py:388` `exporter.build()`（全库打包，实测量级 1.5–2.1s@3000 篇）、`:457` `dbhealth.maintenance()`（VACUUM+REINDEX+**全库重新分词**，2.4–3.4s）、`:739` 备份、`:796` 恢复、`:110` 定时备份；`feature_dialogs.py:1275` `rebuild_fts()` 直调、`:163-189` `inspector.inspect_docx`、`:191-219` 报告导出；`correct_dialog.py:398-425`；`library_panel.py:383-394` |
| **关键反例** | 同文件 `main_window.py:347/516` **已经**用了 `FnWorker` 模式 —— 说明抽象已存在，只是这批调用没用上 |
| **后果** | 万篇库上"重建索引"期间窗口**完全无响应**，用户以为死机。且 `feature_dialogs.py:1271-1277` 的 `_rebuild_fts` **无 try/except**，失败时点按钮**零提示**（同文件 `_sweep_attachments:1274` 有 try/warn）—— 即"维护成功但索引 0 条"的同型缺陷 |
| **改法** | ① 复用 `ui/workers.py::FnWorker`，把上述调用移入 worker + 进度信号；② `rebuild_fts` 加 `progress_cb`；③ `_rebuild_fts` 包 try/except + 失败 `warn`；④ 结果为 0 条时**也显式提示** |
| **涉及** | `ui/feature_dialogs.py`、`ui/main_window.py`、`db/dao.py` |
| **优先级** | **P1**（价值高 / 成本中低）。**与既有文档 P1 项重合，可合并实施** |
| **验证** | 操作期间窗口可交互；断言操作在 QThread 内完成；断言失败路径有提示 |

### N4 退出时后台线程停不掉 → Qt `qFatal` 崩溃

| 项 | 内容 |
| --- | --- |
| **证据** | `main_window.py:32-63` `_wait_all_threads` 遍历 QThread，**先调 `stop()`** 再 `wait(10000)`；但 `ui/workers.py` 中 **`FnWorker`(46)、`CompileWorker`(121)、`PdfRenderWorker`(148)、`BookletWorker`(179) 四个类根本没有 `stop()` 方法**（仅 `ImportWorker:81`、`TTSWorker:214` 有） |
| **后果** | 这四个线程若在跑，`stop()` **静默无效**（大概率被宽 except 吞），10s 后仍 `accept()` 退出 → QThread 存活时进程退出 → Qt 报 `Destroyed while thread is still running`。`main_window.py:886-892` 的注释**自述退出码 0xC0000409**，说明团队已见过此崩溃但未定位到根因 |
| **改法** | ① 四个 worker 补 `stop()`（置 `_cancel` 标志 + 在 `run` 的循环里检查）；② `_wait_all_threads` 对无 `stop()` 的线程改调 `requestInterruption()`；③ 超时后的 `accept()` 改为**先 `terminate()`+`wait(500)` 兜底**，避免悬空线程 |
| **涉及** | `ui/workers.py`、`ui/main_window.py` |
| **优先级** | **P1**（价值高 / 成本中）—— 这是**真崩溃**，不是体验问题 |
| **验证** | 断言四个 worker 均有 `stop()`；构造"退出时有任务在跑"用例，断言进程正常退出 |

### N5 SAPI COM 对象未释放

| 项 | 内容 |
| --- | --- |
| **证据** | `core/tts.py:51`、`:84` 创建 SAPI COM 对象，**无 `pythoncom.CoUninitialize()` / 无显式释放** |
| **后果** | 反复朗读会累积 COM 引用；进程退出时可能报 COM 相关错误。属渐进式资源泄漏 |
| **改法** | 与同仓 `core/compiler.py` / `core/parsers/doc_parser.py` 的 COM 释放写法对齐（那两处已有 `try/finally` 释放在正确位置） |
| **涉及** | `core/tts.py`（约 8 行） |
| **优先级** | **P2**（价值中 / 成本低） |

### N6 异常类名与英文原文直接弹给用户（6 处）

| 项 | 内容 |
| --- | --- |
| **证据** | ① `ui/feature_dialogs.py:242` `self.failed.emit(f"{type(exc).__name__}: {exc}")` → `warn(...)`，用户看到 `UnicodeDecodeError: 'gbk' codec can't decode byte...`；② `:266` `self.done.emit([("正则错误", str(exc), 0)])` → 英文 `re.error` 原文进结果表；③ `:388/435` 同上；④ `ui/reference_panel.py:133` `setText(f"结果渲染失败：{type(exc).__name__}: {exc}")`；⑤ `:284` `"...快照未保存（%s）" % type(exc).__name__` → 用户看到 `OperationalError`；⑥ `ui/workers.py:64/116/143/174/197/228` **六处**裸 `str(exc)` 上传，上游 `compile_wizard.py:294/354/355`、`import_dialog.py:188`、`main_window.py:525`、`editor_panel.py:555` 直接 `info/warn` 出去 |
| **缺口本质** | 目标用户是**文书岗人员**，且**离线内网没有客服** —— **提示就是唯一的售后** |
| **已有范式** | 同仓 `main_window.py:760-779` `_restore_failure_text`（先透出、再按关键字补中文解释）、`feature_dialogs.py:1227-1250` `_pack_post_import_hint`（把"未就绪"翻成三步）—— **抽成公共函数复用即可** |
| **改法** | 新增 `gwtool/ui/errmsg.py::friendly(exc, action)`：按异常类型映射中文可执行文案（`PackageNotFoundError`→"文件可能已损坏…请用 Word/WPS 另存为 .docx"；`PermissionError`→"文件被占用或没有权限"；`OSError` 磁盘满→"磁盘空间不足"），**原始文本只进日志**；上述 6 处统一改调 |
| **涉及** | 新增 `ui/errmsg.py`；`ui/feature_dialogs.py`、`ui/workers.py`、`ui/reference_panel.py`、调用方 |
| **优先级** | **P1**（价值高 / 成本低）—— **与既有文档 U2 合并实施**（U2 只覆盖导入路径，本项覆盖全部 6 处） |
| **验证** | 断言 4 类错误文案**均不含 `Error` / 英文类名** |

### N7 首启模态警告阻塞启动

| 项 | 内容 |
| --- | --- |
| **证据** | `ui/main_window.py:78` 在 `__init__` 内**同步**调 `_first_run_checks()`（`:821-828`）→ 缺字体时 `warn(self, ...)` 弹 `QMessageBox.warning` **模态**，此时窗口**尚未 `show()`** |
| **后果** | 用户第一次启动，**窗口还没画出来先被一个技术性警告拦住**；parent 是未显示窗口，对话框可能出现在屏幕中央而背后无窗口。且 `font_warning_shown=1` 只弹一次 —— **恰恰只在最需要好印象的首启时刻弹** |
| **团队已识别的同型** | `_maybe_db_health_check` 已**刻意**从构造期挪到 `showEvent`（`:841-859` 注释写明线程竞争原因）—— **字体检漏掉了** |
| **改法** | 改为**非模态提示条**（`QStatusBar.showMessage` 常驻 + 状态栏可点击展开详情），不阻塞启动；同时把"一次性"改为**每次生成公文时可见**（与 N9 联动） |
| **涉及** | `ui/main_window.py` |
| **优先级** | **P1**（价值中高 / 成本低）—— **与既有文档 U3 合并** |
| **验证** | 断言构造 `MainWindow` **不被模态框阻塞**（离屏探针在 `MainWindow()` 一行即可跑通） |

### N8 空库 / 空检索无任何引导

| 项 | 内容 |
| --- | --- |
| **证据** | `ui/library_panel.py:132-172` `_reload_docs()`：docs 为空时 `for` 不执行，`doc_list` 保持 `clear()` 后的空白，仅 `count_label` 显示 `"0 篇"`（`:172`）—— **无占位提示、无引导按钮**；`:177-188` 检索无命中同样只清空列表 + `"检索到 0 处"` |
| **对照** | `ui/editor_panel.py:78-81` 空状态文案**质量很好**（"双击左侧材料打开编辑；或直接粘贴文字…"），但**它说"双击左侧"而左侧是空的**；且埋在编辑器里，没告诉用户第一步是"导入" |
| **改法** | `_reload_docs` 在 docs 为空且无检索词时插入**一条不可选中的引导项**（"① 导入材料 → ② 新建公文 → ③ 一键汇编"，含快捷键提示）；检索无命中时显示"没有匹配的材料，试试别的关键词"；编辑器占位同步改为"还没有材料，先点「导入材料」" |
| **涉及** | `ui/library_panel.py`（约 30 行）、`ui/editor_panel.py` |
| **优先级** | **P1**（价值中高 / 成本低） |
| **验证** | 离屏渲染空库，断言存在引导文本 |

### N9 缺字体导致排版不合规，且用户无从得知 ★

| 项 | 内容 |
| --- | --- |
| **证据** | `core/template.py:21-25` 硬编码 `FONT_BODY="仿宋_GB2312"` / `FONT_XBS="方正小标宋简体"`；`core/docxgen.py:44-46` 把字体名**直接写入** `w:ascii`/`w:hAnsi`/`w:eastAsia`，**不检查是否安装** |
| **不对称** | **PDF 侧有完整回退链**（`core/pdfrender.py:97-107` `_font_family` + `:60-94` `ensure_cjk_font` 注入 PyMuPDF china-s），**docx 侧什么都没有** |
| **后果** | GJB 5100A / GB/T 9704 要求正文三号仿宋、标题二号小标宋。缺字体时 WPS/Word **静默替换**字体 → 字宽/行距变化 → **版心 22 行×28 字、固定行距 28 磅实际不成立** → 成品不合规。而用户以为导出成功 |
| **改法（不改默认生成行为，只增可观测性）** | ① 把 `missing_official_fonts()` 从 UI 层下沉到 `core`（避免 UI 依赖）；② `docxgen` 生成前探测，缺失字体写日志；③ 生成完成对话框追加"本机缺少 X 字体，Word 侧可能替换导致排版偏差"；④ UI 字体下拉（`ui/widgets.py:27` `make_font_combo`）对缺失族名加后缀「（本机未安装）」 |
| **涉及** | `core/docxgen.py`、`core/template.py`、`ui/widgets.py`、`ui/compile_wizard.py`、`ui/main_window.py` |
| **优先级** | **P0**（合规是本产品的立身之本 / 成本中低） |
| **验证** | 断言缺失字体时生成结果含告警且日志有记录 |

### N10 `export_dir()` 硬编码英文 `Documents` → 中文桌面失效

| 项 | 内容 |
| --- | --- |
| **证据** | `paths.py:90` `home = Path.home()/"Documents" if Path.home().joinpath("Documents").exists() else Path.home()` |
| **后果** | 麒麟中文桌面用户目录是 `~/文档`，**`~/Documents` 不存在** → 回退 `Path.home()` → 公文输出**散落在家目录根部**，用户找不到导出结果 |
| **改法** | 按序探测候选：`~/Documents` → `~/文档` → `XDG_DOCUMENTS_DIR`（读 `~/.config/user-dirs.dirs`）→ `Path.home()`；取第一个存在的 |
| **涉及** | `gwtool/paths.py`（约 10 行） |
| **优先级** | **P0**（价值高 / 成本极低） |
| **验证** | mock 掉 `~/Documents` 不存在但 `~/文档` 存在，断言返回后者 |

### N11 数据目录无写权限检测 → 只读环境启动即崩且无诊断

| 项 | 内容 |
| --- | --- |
| **证据** | `paths.py:42,46,54,73,84,92,98,111` **7 处** `mkdir(parents=True, exist_ok=True)` **全无 try/except**；`app.py:156` 启动早期调 `dbconn.configure(db_path())` |
| **不对称** | 同文件 `app.py:158-177` 对 `sqlite3.DatabaseError` 有**完整可读诊断**（指向最近备份），但 `PermissionError`/`OSError` **零处理** |
| **后果** | 只读挂载 / 企业策略锁定 / 磁盘满时，程序在最早期抛栈崩溃，且 `--windowed` 下**无控制台** → 用户看到"双击没反应" |
| **改法** | ① `app_data_dir()` 加可写探测（Windows 上 `os.access` 不可靠 → **实际写临时文件再删**）；② `app.py` 启动段把 `OSError`/`PermissionError` 与现有 sqlite 诊断**同构处理**：写 `~/gwtool_启动诊断.log` + 弹可读提示（换 `--portable` / 检查目录权限） |
| **涉及** | `gwtool/paths.py`、`gwtool/app.py` |
| **优先级** | **P0**（价值高 / 成本低） |
| **验证** | 指向只读目录，断言启动给出可读提示而非 stacktrace |

### N12 无 lock 文件 → 间接依赖跨平台漂移

| 项 | 内容 |
| --- | --- |
| **证据** | 直接依赖**已分档**（`requirements.txt:16-17` PySide6 按平台、`:20-27` 按 Python 版本）；**全仓无任何 lock 文件**（无 `.lock`/`pip-compile`/`uv.lock`）；Windows job 用 py3.11（`build.yml:19`），Linux job 用 py3.9（`build.yml:153` Debian 11） |
| **后果** | 同一 `requirements.txt` 在两平台解析出的 **transitive 依赖小版本不同**。而 `requirements.txt:3-7` 记载的事故模式（同源码不同行为）**边界已从直接依赖转移到间接依赖** |
| **改法（渐进，不引重工具）** | ① CI 两个 job 各加一步 `pip freeze > artifacts/requirements-locked-<platform>.txt` 并上传 → 实际构建出的依赖集可追溯；② 复查时对比两份差异，对差异项在 `requirements.txt` 显式加约束 |
| **涉及** | `.github/workflows/build.yml` |
| **优先级** | **P1**（价值中 / 成本低） |
| **验证** | CI 两 job 均产出 freeze 文件 |

---

## 二 既有文档未落地的 18 项（本次复核确认）

证据已在 `v1.6.0增强方向分析与实施方案.md` §二 逐条给出，此处只列**优先级与本次核验状态**：

| 编号 | 项目 | 优先 | 本次核验 |
| --- | --- | --- | --- |
| S2 | 台账 CSV 公式注入（`registry.py:115-127` 原样写值） | P2 | ✅ 未修 |
| D1 | 合法空库静默顶掉真库（`backup.py:694-707` 只查结构） | **P0** | ✅ 未修 |
| D2 | 健康自检盲区（`dbhealth.py` 无 FTS/孤儿交叉检查） | P0 | ✅ 未修（实测 `grep` 无 fts/orphan 检查） |
| D3 | e2e 备份只验文件存在（`e2e_check.py:225` 仅 `exists()`） | P0 | ✅ 未修 |
| D4 | xlsx 工作表名含 `"` → Excel 报损坏（`xlsx.py:69`） | P0 | ✅ 未修 |
| D5 | 迁移失败被吞 + 版本号顶格（`schema.py:281-289/294`） | P1 | ✅ 未修 |
| D6 | 移交包只导出无校验（`exporter.py` 无 `verify_package`） | P2 | ✅ 未修 |
| P1 | 重型操作移出主线程 | P1 | ✅ 未修 → **与 N3 合并** |
| P2 | `exporter.build` O(n²) 重名检查 | P0 | ✅ 未修（`grep "any(e\["` 现无命中，需重新定位实现） |
| P3 | 批量归档逐行 commit（`archive.py:83`） | P1 | ✅ 未修 |
| P4 | `rebuild_fts` 增量化（可复用 `text_hash` 列） | P2 | ✅ 未修 |
| F1 | 快照失败仍覆盖原文（`batch.py:431-434`） | P1 | ✅ 未修 |
| F2 | 备份列表/恢复静默降级（`backup.py:499/587`） | P1 | ✅ 未修 |
| F3 | 设置类写失败无痕（`csc_gec.py:170`/`reminder.py:39`） | P2 | ✅ 未修 |
| U1 | 批量导入失败原因被吞（`workers.py:98-100`） | P0 | ✅ 未修 |
| U2 | 错误文案暴露异常类名 | P0 | ✅ 未修 → **与 N6 合并** |
| U3 | 空库首启无引导 | P1 | ✅ 未修 → **与 N7+N8 合并** |
| U4 | 设置对话框无滚动区 | P1 | ✅ 未修 → **与 N2 合并（同一处）** |
| U5 | tooltip 覆盖不足（主窗口 18 动作仅 3 个有；`ui/` 全目录 34 处；菜单 32 项 0 tooltip） | P1 | ✅ 未修 |
| U6 | 汇编选材无筛选且默认全勾 | P2 | ✅ 未修 |

> **U4 与 N2 是同一处**（SecurityDialog）；**U2 与 N6 是同一处**（错误文案）；**U3 与 N7+N8 同族**（首次体验）；**P1 与 N3 同一处**（主线程重活）。

---

## 三 改进实施计划（全 30 项 · 6 次提交）

> **编排原则**：功能一次做全、但**按批分 6 次提交**。每批结束跑完三条门禁再进下一批，
> 这样"一次性交付"仍有 6 个可精确回退的点位。
> 每批内部的**改动顺序**按"先加护栏 → 再改行为"排列，避免中间态失衡。

### B1 / 6 —— 零风险修复（8 项）

不动架构、不碰 schema、不动打包，全部是函数内改动。

| 序 | 项 | 文件 | 改动要点 |
| --- | --- | --- | --- |
| 1 | **N1** 主窗口尺寸夹取 | `ui/main_window.py` ~6 行 | 用 `availableGeometry()` 夹取 ≤90%，并设 `setMinimumSize(1024, 640)` |
| 2 | **N10** `export_dir` 中文桌面 | `paths.py` ~10 行 | 按序探测 `~/Documents` → `~/文档` → `XDG_DOCUMENTS_DIR`（读 `~/.config/user-dirs.dirs`）→ `Path.home()` |
| 3 | **N2 + U4** SecurityDialog 滚动区 | `ui/feature_dialogs.py` ~12 行 | 照抄 `receive_dialog.py:105-110`；高度改 `min(720, 可用高-80)`；顺检 `correct_dialog.py:40` |
| 4 | **D4** xlsx 工作表名引号 | `core/xlsx.py:69` | 补清洗 `"` 与控制字符；XML 属性改用 `quoteattr`；顺核 `:243` 外所有属性拼接点 |
| 5 | **S2** CSV 公式注入 | `core/registry.py:115-127`、`core/receive.py` | 以 `= + - @` 起首的值加前置 `'`；**不动 xlsx 通路**（inlineStr 本身安全） |
| 6 | **D3** e2e 备份往返断言 | `scripts/e2e_check.py:225` ~15 行 | 备份后补"恢复 → 断言文档数与标题集合一致"；**必须先 `paths._override` 到临时目录** |
| 7 | **P2** exporter O(n²) | `core/exporter.py` | 重名检查改 `seen: set[str]`；`exists() and is_file()` 合并为一次 `stat` |
| 8 | **F3** 设置写失败留痕 | `core/csc_gec.py:170`、`core/reminder.py:39` | 补 `logs.warning` + 调用方提示；顺检其他 `set_setting` 静默点 |

> ⚠️ **P2 的定位已变化**：`grep "any(e\[" gwtool/core/exporter.py` 现**无命中**，
> 说明实现已重构。实施时**先重新定位重名检查的实际写法**，再决定是否仍需改，**不要照抄行号**。

### B2 / 6 —— 数据安全与核心体验（6 项）

| 序 | 项 | 文件 | 改动要点 |
| --- | --- | --- | --- |
| 9 | **D1** 合法空库告警 | `core/backup.py:694-707`、`ui/feature_dialogs.py` | 换库**前**只读比较包内与当前库 `count(documents)`（及可选 `count(receive_register)`）；包内为 0 而当前非 0 或降幅 >50% → 写 `RestoreReport` 告警字段 → UI 二次确认 |
| 10 | **F1** 快照失败可见 | `core/batch.py:431-434`、`ui/feature_dialogs.py:271-274` | 抽公共 helper：`logs.warning` + `result.failures` 追加"（标题，正文已改，但回滚快照未保存：…）" |
| 11 | **F2** 备份列表显示损坏包 | `core/backup.py:499/587` | `list_backups` 捕获后 append `{"file","corrupt":True,"error"}` 而非 `continue`；`restore_backup_detailed` 附件失败并入 `RestoreReport` |
| 12 | **D2** 自检覆盖交叉一致性 | `core/dbhealth.py:74-88` | 加 3 条**只读**查询：① FTS 行数 vs `documents` 行数；② 附件 `doc_id` 悬空；③ 快照 `doc_id` 悬空。失配时给一键修复入口（复用 `dao.rebuild_fts` / `attachments.sweep_orphans`） |
| 13 | **N11** 写权限检测 + 启动诊断 | `paths.py`、`app.py:156` | `app_data_dir()` 加可写探测（**实际写临时文件再删**，Windows `os.access` 不可靠）；`app.py` 把 `OSError`/`PermissionError` 与现有 sqlite 诊断同构处理：写 `~/gwtool_启动诊断.log` + 可读提示（换 `--portable`） |
| 14 | **U1** 批量导入失败清单 | `ui/workers.py:98-100`、`ui/import_dialog.py:193` | `ImportWorker` 收集 `(文件名, 原因)` 经新 signal 回传；`_done` 展示前 8 条 + 「导出失败清单」按钮；原因文案**复用 `importer.py` 已有中文串**（`:29-31`/`:66` 精心写好的 OCR 指引目前在批量路径永远看不到） |

### B3 / 6 —— 错误文案治理（2 项，同族合并）

| 序 | 项 | 文件 | 改动要点 |
| --- | --- | --- | --- |
| 15 | **N6 + U2** 错误文案治理 | **新增** `ui/errmsg.py`；改 `ui/feature_dialogs.py:242/266/284/388/435`、`ui/workers.py:64/116/143/174/197/228`、`ui/reference_panel.py:133`；`core/importer.py` 加 `_friendly_error` | `friendly(exc, action)` 按异常类型映射中文可执行文案：`PackageNotFoundError`→"文件可能已损坏或不是有效的 Word 文档，请用 Word/WPS 打开后另存为 .docx 再试"；`FileNotFoundError`；`PermissionError`；磁盘满。**原始文本只进日志**。抽公共函数时**复用同仓既有范式**：`main_window.py:760-779` `_restore_failure_text`、`feature_dialogs.py:1227-1250` `_pack_post_import_hint` |
| 16 | **N9** 字体可观测性 | `core/docxgen.py:44-46`、`core/template.py:21-25`、`ui/widgets.py:27`、`ui/compile_wizard.py` | ① `missing_official_fonts()` 从 UI 层**下沉到 core**（避免 UI 依赖）；② `docxgen` 生成前探测 + 缺失写日志；③ 生成完成对话框追加告警；④ `make_font_combo` 对缺失族名加「（本机未安装）」 |

> **为什么 N9 重要**：`docx` 侧**无任何回退**，而 PDF 侧有完整回退链
> （`core/pdfrender.py:97-107` `_font_family` + `:60-94` `ensure_cjk_font`）——**这种不对称本身就是缺陷**。
> 缺字体时 WPS/Word 静默替换 → 版心 22 行×28 字、固定行距 28 磅**实际不成立** → 成品不合规。

### B4 / 6 —— 响应性与崩溃修复（4 项）

| 序 | 项 | 文件 | 改动要点 |
| --- | --- | --- | --- |
| 17 | **N4** worker `stop()` 补齐 ★ | `ui/workers.py:46/121/148/179`、`ui/main_window.py:32-63` | `FnWorker`/`CompileWorker`/`PdfRenderWorker`/`BookletWorker` **四个类补 `stop()`**（置取消标志 + `run` 循环内检查）；`_wait_all_threads` 对无 `stop()` 者改调 `requestInterruption()`；超时后的 `accept()` 前加 `terminate()+wait(500)` 兜底 |
| 18 | **N3 + P1** 重型操作移出主线程 | `ui/main_window.py:388/457/739/796/110`、`ui/feature_dialogs.py:1275/163-189/191-219`、`db/dao.py` | 复用已有 `ui/workers.py::FnWorker`（`main_window.py:347/516` 已是此模式）；`rebuild_fts` 加 `progress_cb`；`_rebuild_fts` 包 try/except + 失败 `warn`；结果为 0 条时**也显式提示** |
| 19 | **P3** 批量归档单事务 | `core/archive.py:83` | `batch_archive` 内集中 commit / `executemany`；顺检 `core/batch.py` 批量纠错是否有同类逐行 commit |
| 20 | **D5** 迁移全成功才写版本号 ⚠️ | `db/schema.py:281-289/294`、`db/connection.py:114` | ① 迁移链入**单事务**，全成功才写 `user_version`；② 打开时若 `ver > SCHEMA_VERSION` → 先兜底备份 + 明确提示"此库由更新版本创建"，**不再改写版本号** |

> ⚠️ **D5 触碰迁移链，必须单独成一次提交**（即 B4 内拆出两个 commit）。
> **N4 是真崩溃修复**，不是体验问题：`main_window.py:886-892` 注释自述退出码 `0xC0000409`，
> 团队已见过此崩溃但未定位到根因 —— 根因即"四个 worker 无 `stop()`"。

### B5 / 6 —— 首次体验与可发现性（4 项）

| 序 | 项 | 文件 | 改动要点 |
| --- | --- | --- | --- |
| 21 | **N7 + U3** 首启非模态提示 | `ui/main_window.py:78/821-828` | 字体检从 `__init__` 内同步模态**改为状态栏常驻提示**（不阻塞启动）；把"只在首次"改为**每次生成公文时可见**（与 N9 联动）。**对齐 `_maybe_db_health_check` 已挪到 `showEvent` 的做法**（`:841-859`） |
| 22 | **N8** 空态引导 | `ui/library_panel.py:132-172/177-188`、`ui/editor_panel.py:78-81` | `_reload_docs` 在 docs 空且无检索词时插入**不可选中的引导项**（"① 导入材料 → ② 新建公文 → ③ 一键汇编"）；检索无命中显示"没有匹配的材料，试试别的关键词" |
| 23 | **U5** tooltip 补齐 | `ui/main_window.py:142-192/217-262`、`ui/compile_wizard.py`、`ui/receive_dialog.py` | 主窗口 **18 个动作 15 个无 tooltip**（仅 `:173/177/181` 三个有）；**菜单 32 项 0 tooltip**；汇编向导三步、收文表单关键字段（"拟办意见"vs"领导批示"、"保管期限"）各补一句。**零依赖、零网络** |
| 24 | **N12** CI freeze 清单 | `.github/workflows/build.yml` | 两个 job 各加 `pip freeze > artifacts/requirements-locked-<platform>.txt` 并上传 |

### B6 / 6 —— 储备与收尾（4 项）

| 序 | 项 | 文件 | 改动要点 |
| --- | --- | --- | --- |
| 25 | **P4** `rebuild_fts` 增量 | `db/dao.py::rebuild_fts` | 按 `documents.text_hash` 比对，仅对变更/缺失行重分词（DELETE+INSERT 影子表） |
| 26 | **D6** 移交包校验 | `core/exporter.py`、`ui/main_window.py` | 新增只读 `verify_package()`：逐条校验条目存在 + `sha256` 匹配 + manifest 计数一致；UI 在"批量导出与移交包"旁提供「校验移交包」入口。**导入回程可后置** |
| 27 | **U6** 汇编选材筛选 | `ui/compile_wizard.py:41-49` | 加分类/标签筛选 + 全选/全不选/反选。**当前默认全勾反而危险**（易误编无关材料） |
| 28 | **N5** SAPI COM 释放 | `core/tts.py:51/84` | 对齐 `core/compiler.py` / `core/parsers/doc_parser.py` 的 COM 释放写法（已有 `try/finally`） |

> 说明：30 项与批次表的对应关系为 —— B1 含 8 项（其中 N2+U4 合并计为 1 项、实为 2 项口径），
> B2 6 项、B3 2 项、B4 4 项、B5 4 项、B6 4 项；按**合并后条目**共 28 条，覆盖原始 30 项
> （U4 并入 N2、U2 并入 N6、U3 并入 N7、P1 并入 N3）。

---

## 三点五 强制前置：每批执行前的三条纪律

1. **新回归测试先在旧实现上确认失败（红 → 绿）** —— 本项目已验证过的陷阱：
   `test_diagpack.py:102` `test_pack_contains_no_document_text` 只把标记写进**数据库**、
   **从不写日志**，导致日志通道**零覆盖**却 20 个用例全过。**照抄式断言会掩盖缺陷**。
2. **涉及 UI 的批次先跑 `tests/test_ui_actions_smoke.py`**，确认新增弹框入口的 mock 已补齐
   —— PySide6 静态方法（`QFileDialog`/`QInputDialog`）是 **C++ 侧实现，不走 Python 层 `QDialog.exec`**，
   `monkeypatch.setattr(QDialog, "exec", ...)` **拦不住**，会真弹框把测试**挂死**
   （实测：全量跑停在 83%、日志 15 分钟不增一行、无报错）。
3. **每批结束立即跑三条门禁**（下节），全绿才进下一批。

---

## 四 每批交付前必跑的门禁

```bash
export PATH="/c/Windows/System32:/usr/bin:/bin:$PATH"
cd C:/gwtool
.venv/Scripts/ruff.exe check .                                 # 全仓库（含 main.py）
PYTHONPATH= .venv/Scripts/python.exe -m pytest tests/ -q       # 基线 1127 passed / 3 skipped
PYTHONPATH= .venv/Scripts/python.exe scripts/e2e_check.py      # 20 项
```

**本机环境注意**

- `ruff` 用 `.venv/Scripts/ruff.exe`，**`python -m ruff` 不可用**
- `pytest` 与 `e2e` **必须加 `PYTHONPATH=` 前缀** —— 沙箱经 `PYTHONPATH` 注入
  `sitecustomize.py` 劫持 `shutil.rmtree`，单次删 >50 文件会抛
  `SAFE_DELETE_BULK_CONFIRM_REQUIRED` 并 `SystemExit(1)`，**不加前缀会得到假失败**
- 每条命令前 `export PATH="/c/Windows/System32:/usr/bin:/bin:$PATH"`

**工程纪律（本项目硬约束）**

- **新回归测试必须先在旧实现上确认失败（红 → 绿）**，避免写出恒过的断言
- **e2e 与任何破坏性探针必须先 `paths._override` 到临时目录**，绝不碰用户真实数据
- **临时脚本/日志写到仓库外**（写进仓库会被 `test_no_hardcoded_build_machine_paths` 全库扫到；
  注意该测试**会扫 `.md`**，文档里写 `C:/gwtool`（反斜杠）会让门禁变红，写 `C:/gwtool` 才安全）
- **UI 测试新增弹框入口必须同步 `tests/test_ui_actions_smoke.py` 的 mock**
- **触碰 schema 的改动单独提交**（D5）
- **跑测试挂住时**：`pytest -u ... -v > log` 看停在哪条（`-u` 必加，否则行缓冲吞掉线索）；
  `pytest-timeout` 已装，可用 `--timeout=60`

---

## 五 明确不做（继承既有结论，不重复论证）

**6 项架构级不做**：云同步 / 大模型改写 / 换架构 / 引大依赖 / 常驻进程 / 用户可编辑正则。

**本轮新增的"不做"**（均有实测依据）：

| 候选 | 为什么不建议 |
| --- | --- |
| 列表分页 / 虚拟列表 / 表格虚拟化 | 实测 3000 篇列表 18.8ms、收文台账 71ms，**不需要** |
| XLSX 改流式 / 拆分临时文件 | 3000 行 272ms、峰值 19MB 可接受；"不用临时文件"是**原子性设计** |
| WAL / checkpoint 调优 | 500 次小写后 7.4MB，checkpoint 归 0；`maintenance` 已 TRUNCATE |
| 启动期预加载 jieba | 880ms 但是**惰性**的；预加载是把成本提前到用户不需要时付 |
| 改 `synchronous=NORMAL` | 掉电可丢最近事务，属性能/持久性权衡，不擅自改默认 |
| 把 `CORE_TABLES` 扩成全表 | 会**重演**"新增一张表→用户旧备份全不可恢复"的陷阱 |
| 静默吞咽一刀切改 `raise` | 67 处中约 55 处是**正确的降级设计** |
| 在线文档 / 教程视频 / FAQ 网页 | 落在"内网零网络"红线外，用户**物理上打不开** |
| 为台账加"回收站" | 现有二次确认已写明"不可撤销"，成本高于收益 |
| 主题跟随系统的完整适配（N 未列） | 待麒麟实机验证后再定，先做实测 |

---

## 六 关键文件清单

| 文件 | 涉及项 |
| --- | --- |
| `gwtool/ui/main_window.py` | N1 N3 N4 N7 U5 |
| `gwtool/ui/feature_dialogs.py` | N2 N3 N6 U4 U5 |
| `gwtool/ui/workers.py` | N4 N6 U1 |
| `gwtool/ui/library_panel.py` | N8 |
| `gwtool/ui/errmsg.py`（**新增**） | N6 U2 |
| `gwtool/paths.py` | N10 N11 |
| `gwtool/app.py` | N11 |
| `gwtool/core/docxgen.py`、`core/template.py` | N9 |
| `gwtool/core/backup.py` | D1 F2 |
| `gwtool/core/dbhealth.py` | D2 |
| `gwtool/core/xlsx.py` | D4 |
| `gwtool/core/registry.py`、`core/receive.py` | S2 |
| `gwtool/core/exporter.py` | P2 D6 |
| `gwtool/core/batch.py`、`core/archive.py` | F1 P3 |
| `gwtool/core/csc_gec.py`、`core/reminder.py` | F3 |
| `gwtool/core/importer.py` | U2 |
| `gwtool/core/tts.py` | N5 |
| `gwtool/db/schema.py`、`db/connection.py` | D5 |
| `gwtool/db/dao.py` | N3 P4 |
| `scripts/e2e_check.py` | D3 |
| `.github/workflows/build.yml` | N12 |

---

## 七 验证判据总表

| 项 | 关键判据（**必须能红 → 绿**） |
| --- | --- |
| N1 | 构造后 `height() <= availableGeometry().height()` |
| N2 | `SecurityDialog` 内存在 `QScrollArea` 且高度受屏限制 |
| N4 | 断言 4 个 worker 均有 `stop()`；"退出时有任务在跑"用例断言进程正常退出 |
| N5 | 反复朗读后 COM 引用不累积 |
| N6 | 4 类错误文案**均不含 `Error` / 英文类名** |
| N7 | 构造 `MainWindow` **不被模态框阻塞** |
| N8 | 离屏渲染空库，断言存在引导文本 |
| N9 | 缺失字体时生成结果含告警且日志有记录 |
| N10 | mock `~/Documents` 不存在但 `~/文档` 存在 → 断言返回后者 |
| N11 | 指向只读目录 → 断言给可读提示而非 stacktrace |
| N12 | CI 两 job 均产出 freeze 文件 |
| D1 | 用"合法空库"恢复 → **必须出现告警**；现有 `test_backup_compat.py` 不回归 |
| D2 | 注入 FTS 失配 → 自检**必须非 ok**；修复后回 ok |
| D3 | 注入"备份=空库"缺陷 → **e2e 必须变红** |
| D4 | 引号文件名导出 → **全部件可被 XML 解析器解析** |
| D5 | 迁移失败 → **抛错且版本不变**；新库被旧程序打开 → 版本不被改写 |
| D6 | 篡改包内任意字节 → `verify` **必须失败**；未篡改包必须通过 |
| P2 | 前后耗时对比；重名文档仍正确追加 `_id` 后缀 |
| P3 | 探针对比前后耗时；`archived`/`skipped` 计数一致 |
| P4 | 连续两次重建，第二次 < 0.2s 且条数不变 |
| S2 | 断言导出行首被转义；往返导入后原值不变 |
| F1 | 快照失败 → `failures` **非空**且日志有记录 |
| F2 | 放损坏包 → **出现在列表且被标记**；恢复报告含附件失败项 |
| F3 | 令写库失败 → 断言出现提示 |
| U1 | 5 份损坏/不支持文件 → **逐条清单**出现，文案不含异常类名 |
| U5 | 断言关键动作 `toolTip()` 非空 |
| U6 | 筛选后列表项数正确、全选/反选生效 |

---

## 八 交付后建议的收尾动作

1. **归档本计划**为仓库文档（如 `系统缺陷分析与改进实施记录.md`），与既有 5 份方案文档并列。
2. **重打包并发版** —— 注意 `v1.6.0` 的 Release 目前仍是**替换 12 文种之前**的产物
   （Request C 遗留项，尚未处理），本机测试计数亦已从 1118 变为 1127。发版前需确认基线一致。
3. **麒麟 ARM64 实机验收** —— `麒麟ARM64完整实现方案.md` §五 的 6 步清单**至今未做**，
   而本次 N2/N10/N11/U5 中多项（滚动区、中文桌面路径、写权限诊断、深色主题）**恰是需要实机才能确认**的。
4. **CI 全绿确认** —— 改动涉及 `.github/workflows/build.yml`（N12），
   推送后需确认 Windows x64 与麒麟 ARM64 两个 job 均通过。
