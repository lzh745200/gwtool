# 项目文件结构说明（写给第一次接触本项目的朋友）

> 本文档面向**没有编程背景或第一次看这个项目**的读者。
> 读完你将明白：这个软件由哪些文件组成、每个文件是干什么的、
> 数据在程序里怎么流动、以及"我想改某个功能应该去哪个文件"。

---

## 一、先建立整体印象：这个项目像一栋办公楼

**公文汇编助手**是一个安装在本机、不需要联网就能使用的公文写作软件
（技术底子是 Python 语言 + PySide6 界面库 + SQLite 本地数据库）。

把项目想象成一栋办公楼，你会更容易记住每个目录的角色：

```
                ┌─────────────────────────────────┐
                │   公文汇编助手办公楼             │
                └─────────────────────────────────┘

  main.py    ← 大门（大门朝哪开？双击它程序就启动了）
  gwtool/    ← 办公楼主楼，里面分三层：
    ├─ app.py      ← 前台接待（开机第一步：备好资料、开灯、开门）
    ├─ db/         ← 档案室（所有材料、词典、模板都存这里的数据库）
    ├─ core/       ← 业务科室（真正干活的师傅：解析、纠错、排版、装订…）
    ├─ ui/         ← 窗口服务大厅（你看到的每一个界面都在这里）
    └─ resources/  ← 出厂自带物资（一部 12 万条的词典 + 4 万条纠错对）
  tests/     ← 质检科（465 道考题，每次改完代码都要考一遍）
  scripts/   ← 后勤部（打包、安装、生成词典等杂活）
```

**记住这张图，下面每一节就是把每个房间打开给你看。**

---

## 二、看懂目录树（总览）

```
gwtool/                          ← 项目根目录（整个项目就放在这里）
├── main.py                      ★ 程序入口：双击运行的就是它
├── gwtool.spec                  打包说明书（把程序做成 exe 用）
├── requirements.txt             购物清单（程序依赖哪些第三方库）
├── ruff.toml                    代码体检规则（防止低级错误）
├── README.md                    项目说明书（功能总览 + 使用方法）
├── 项目文件结构说明.md            ← 本文档
├── open-code-review审查报告.md   全项目代码审查记录（范围/真伪甄别/修复/验证）
├── gwtool/                      ★ 主包：全部源代码都在这里（76 个模块）
│   ├── __init__.py              包的"门牌"（写着软件名和版本号 v1.6.0）
│   ├── app.py                   启动总管
│   ├── paths.py                 管家：决定数据存到哪
│   ├── logs.py                  运行期诊断日志（脱敏、非阻塞、有界轮转）
│   ├── db/                      数据层（5 个文件）
│   ├── core/                    逻辑层（47 个文件 + parsers 子目录 7 个）
│   ├── ui/                      界面层（20 个文件）
│   └── resources/data/
│       └── seed.db              种子库（15.7 MB，出厂自带词典+纠错对）
├── tests/                       测试套件（63 个测试文件，1190 个用例）
├── scripts/                     构建与运维脚本（11 个 Python 脚本 + 平台脚本）
└── .github/workflows/build.yml  云端自动构建流水线（GitHub Actions）
```

带 ★ 的是最重要的三个位置，其他都是给它们"打下手"的。

---

## 三、根目录文件逐个说明

| 文件 | 作用（大白话） | 你什么时候需要碰它 |
|------|----------------|--------------------|
| `main.py` | 程序的**总入口**。支持三种启动方式：`python main.py`（正常启动）、`--portable`（便携模式，数据存 U 盘）、`--import 某文件`（启动时顺手导入一个文件，配合 Windows 右键菜单用） | 平时不用改；想加命令行参数才动它 |
| `gwtool.spec` | 打包成 exe 的"装箱单"：带哪些资源（如种子库 seed.db）、排除哪些用不到的大模块（如网络组件），让成品又小又干净 | 需要打包发布 exe 时 |
| `requirements.txt` | 项目依赖的第三方库清单（PySide6 画界面、PyMuPDF 处理 PDF、jieba 分词等 17 项） | 装环境时 `pip install -r requirements.txt` |
| `ruff.toml` | 代码静态检查规则，开了四组：语法错误（E9）、Pyflakes 全组（未用导入/重复定义/多余 f 前缀，F）、py39 注解兼容（FA100/FA102）、bugbear 隐患（B）。目的是拦住"一打开对话框就崩""麒麟上跑不起来""垃圾代码回归"这类缺陷 | 一般不动；想加规则先全库实测误报量 |
| `README.md` | 项目总说明：功能表、快速开始、打包方法、验收对照表 | 想了解功能时先读它 |
| `open-code-review审查报告.md` | 用 alibaba/open-code-review 做的全项目代码审查记录：审查范围、逐条真伪甄别、12 处缺陷修复、验证结果 | 关心"代码质量怎么保证的"时读它 |
| `.gitignore` | 告诉版本管理工具哪些文件不用保存（缓存、临时文件、打包产物） | 不用碰 |

---

## 四、gwtool/ 主包——软件本体

### 4.1 app.py —— 启动总管

程序从 `main.py` 进门后，实际流程由它安排，顺序是：

1. 适配高分屏/深色模式（Windows 默认强制浅色，保证显示一致）；
2. **首次运行种子导入**：把随包的 `seed.db`（12.3 万词条典 + 4 万条纠错对）批量搬进用户数据库。搬过一次就打标记，下次启动直接跳过（幂等，重复执行也不会重复导入）；
3. 如果设置了**口令锁**，先弹解锁窗口，输错 5 次强制等 10 秒；
4. 拉起主窗口 `MainWindow`，进入界面。

> 类比：前台接待员。每天开门第一件事——查库存、补货、验证件，然后才让客人进大厅。

### 4.2 paths.py —— 数据放哪，它说了算

| 数据 | Windows 默认位置 | 便携模式 |
|------|------------------|----------|
| 数据库 | `%APPDATA%\gwtool\gwtool.db` | 程序同级 `Data\` 文件夹 |
| 附件 | `%APPDATA%\gwtool\attachments\` | 同上 |
| 备份 | `%APPDATA%\gwtool\backups\` | 同上 |
| 日志 | `%APPDATA%\gwtool\logs\` | 同上 |
| 汇编输出 | `我的文档\公文汇编输出\` | 同左 |

> 设计要点：**程序和数据分离**。升级/重装软件不影响用户数据；
> 便携模式则全部塞进 U 盘，即插即用。

---

## 五、db/ —— 档案室（数据层，4 个文件）

| 文件 | 大白话职责 |
|------|-----------|
| `schema.py` | **库房图纸**：定义 9 张表（文档、分类、词典、纠错对、常用句式、模板、设置、快照、忽略名单）+ 3 张全文检索虚表；还负责老版本数据库自动升级（升级前先自动备份一份） |
| `connection.py` | **门禁系统**：管理数据库连接。SQLite 规定"一个线程一把钥匙"，这里保证每个后台线程拿各自的连接；开启 WAL 模式让读写互不卡顿 |
| `dao.py` | **唯一的档案管理员**（551 行，最大的数据文件）：所有存取都必须经它的手——存文档（自动算内容指纹去重）、查文档、管分类、记快照（每篇留最近 30 版）、模板增删改……别的代码不许直接摸数据库 |
| `tokenize.py` | **切词员**：中文要切成词才能高效检索。它用 jieba 把句子切成词、空格连接后存进检索索引；你搜索"乡村振兴 政策"时，也是它把查询切成 `"乡村" "振兴" "政策"` 三个关键词 |

> 为什么要 FTS5 虚表？SQLite 自带的全文检索按空格分词，对付不了中文。
> 所以本项目"预切词再入库"，检索 20 万字资料也**不到 1 秒**。

---

## 六、core/ —— 业务科室（逻辑层，47 个文件，不含任何界面代码）

这是**最核心、代码量最大**的目录。按功能分成 6 组来讲。

### 6.1 解析组：把各种格式的文件"读进来"

> v1.4.0 起支持 **.wps（WPS/MS Works）**：按内容嗅探路由——OOXML zip 走 docx
> 解析器；OLE 复合文档走 .doc 四级降级链（COM 探测金山 WPS 优先，保真最高）。


| 文件 | 作用 |
|------|------|
| `model.py` | 统一中间格式 `DocTree`。不管原文是 Word、PDF 还是网页，读进来都变成同一种"标题+段落+列表+表格"的结构，后面所有功能都只认这一种格式 |
| `importer.py` | 导入调度员：看文件扩展名，指派给对应的解析器；PDF 没有文字层时自动转 OCR，缺中文包时给出明确安装指引而不是默默失败 |
| `parsers/docx_parser.py` | 读 Word（.docx）：按顺序保留标题层级、段落、**表格** |
| `parsers/doc_parser.py` | 读老版 Word（.doc，97-2003 二进制格式）：四级降级策略——① 调用本机 WPS/Word 转换 → ② 调 LibreOffice 转换 → ③ 纯 Python 硬解二进制 → ④ 原始扫描兜底，尽量把文字捞出来 |
| `parsers/pdf_parser.py` | 读文字版 PDF：统计全文最常见字号当"正文字号"，比它大 25% 的判为标题 |
| `parsers/txt_parser.py` | 读 TXT：自动探测编码（GBK/UTF-8/BIG5……），识别"一、（一）1."等公文标题行 |
| `parsers/rtf_parser.py` | 读 RTF 富文本 |
| `parsers/md_html_parser.py` | 读 Markdown 和 HTML（网页另存、复制来的内容都能收） |

### 6.2 生成组：把材料"写成正式公文"

| 文件 | 作用 |
|------|------|
| `skeletons.py` | **12 种军队机关公文文种骨架**（命令/通令/决定/指示/通知/通报/报告/请示/批复/函/通告/纪要），逐条对应《军队机关公文处理工作条例》第八条。填上单位、事由就能得到一篇结构规范的初稿 |
| `template.py` | 排版参数模型：页边距 37/35/28/26 毫米、正文仿宋三号 28 磅行距、红头、页码奇右偶左……默认值按军队机关公文格式设定（页面参数与 GB/T 9704-2012 一致） |
| `docxgen.py` | Word 生成引擎（354 行）：封面→红头→目录→正文→版记一气呵成；目录用真正的 Word 域代码（打开时自动刷新页码）、页码奇偶分侧，**WPS 打开不跑版** |
| `compiler.py` | 汇编总指挥：接收"哪些材料+哪个模板"，调 docxgen 出 Word；Word 转 PDF 有两级备选（LibreOffice → WPS/Word），都不在时提示改用内置渲染器 |
| `pdfrender.py` | **内置 PDF 渲染器**（351 行，不依赖任何 Office 软件）：用"两遍渲染法"解决目录页码难题——第一遍用占位页码排版（保证分页不变），数出每个标题的真实页码，第二遍写真实页码；最后盖"奇右偶左"页码 |
| `booklet.py` | A3 骑马订小册子：8 页手册打印顺序是 `8,1 | 2,7 | 6,3 | 4,5`——算法自动补空白页、算好每张纸正反面印什么，折叠装订后恰好 1~8 页 |
| `batch.py` | 批量汇编：每份材料各自独立成文；**一份失败不影响其他**，最后给出成功清单+失败原因 |
| `watermark.py` | 水印与密级标注："征求意见稿""秘密★1年"斜向平铺；PDF 用旋转矩阵，Word 往页眉注入标准 VML 艺术字水印 |

### 6.3 质检组：找错别字、查格式、比差异

| 文件 | 作用 |
|------|------|
| `corrector.py` | **纠错引擎**（三级流水线）：① 精确匹配 4 万条错别字对 → ② 上下文规则（"截止日期"对、"截止 8 月底"应改"截至"）→ ③ 标点/数字国标规则。还有三道防误报防线：jieba 分词边界保护（"安全生产"里不会误报"全生"）、"以德报怨"等例外白名单、《书名号》内文字不纠。**分词不可用时门控降级为不过滤**（宁可多报也不静默漏报） |
| `corrector_data.py` | 纠错引擎的内置弹药库：221 条人工精标对（含新华社禁用词规范）、52 条机构沿革（"环保局→生态环境局"）、上下文/标点/数字三组规则表 |
| `csc_neural.py` | **L4 神经精排层**（可选）：对前三层没覆盖的片段做一次模型级判定；载体是 ONNX（不引 torch），模型**不随主包分发**，未导入增强包时全程静默、行为与三级流水线一致 |
| `enhance_pack.py` | 精度增强包的导入/校验/卸载：sha256 核对、zip-slip / zip-bomb 防护、staging 原子替换、必填 license 字段 |
| `inspector.py` | **军队公文格式体检**：标题编号链条（跳号、重复、回退）、发文字号必须用六角括号〔〕、成文日期写法、"妥否，请批示"等结束语与文种匹配、"报告"里夹带请示事项报警；还能检查 Word 文件的字体字号行距 |
| `differ.py` | 文档对比：段落级对齐 + 词级精确标红标绿，输出双栏红绿视图和相似度百分比 |
| `simhash.py` | 相似查重：SimHash 指纹粗筛（快）→ 字符三元组 Jaccard 精算（准），两级配合在海量材料中找出"换了个说法的重复件" |

### 6.4 检索与参考组

| 文件 | 作用 |
|------|------|
| `reference.py` | 写作参考：同时查"资料库、词典、常用句式"三个库，各组分数归一到 0~1 再合并排序，双击即插入光标处 |

### 6.5 辅助工具组

| 文件 | 作用 |
|------|------|
| `toolbox.py` | 文秘工具箱：人民币大写（按人民银行票据规范）、日期大写（二〇二六年八月三十日）、全半角切换、OpenCC 简繁转换 |
### 6.4bis 发文登记与增值组（v1.3+ 新增）

| 文件 | 作用 |
|------|------|
| `registry.py` | 发文登记台账：发文字号解析与自动取号、字段校验、统计聚合、CSV 导出（Excel 友好的 UTF-8-BOM） |
| `ruleset.py` | 纠错规则集导入导出：单位规范词库 CSV 回环保真（来源/置信度/停用态），导入后纠错缓存立即失效。现已支持分隔符嗅探（逗号/制表符/分号）、`#` 注释行、**整批单事务**与**逐行错误原因** |
| `wordlist.py` | **词表导入内核**：预检 → 预览 → 单事务写入。把"这些条目与库里现有数据怎么相处"（冲突规则、事务性、可回退）收敛到一处；格式差异全部推给 `wordfmt/` |
| `wordfmt/`（子包） | 词表格式解析层，统一产出「条目列表」：`csv_tsv.py`（CSV/TSV/TXT，多编码+分隔符嗅探）、`json_fmt.py`（分组/裸数组/aliases 展开）、`xlsx_fmt.py`、`docx_fmt.py`（Word 表格）、`tbx_fmt.py`（TBX/XLIFF）、`tabular.py`（表格式公共的表头映射与角色解释） |
| `xlsx_read.py` | **xlsx 读取（零依赖、只读）**。xlsx 本质是 ZIP + XML，用标准库手写最小 OOXML 解析即可：共享字符串（含富文本）、inlineStr、公式缓存值、`r:id` 间接寻址工作表。**刻意不做** `.xls`/日期序列号/样式/图表 |
| `report.py` | 军队公文格式体检报告导出：体检结果组织成 DocTree 复用 docxgen，输出规范 DOCX 报告 |
| `attachments.py` | 文档附件管理：复制进数据目录随库走、文件名跨平台清洗、重名不覆盖、路径越界拒绝、孤儿清理 |
| `classify.py` | 智能分类建议：按既有分类词频画像给新材料推荐归类（只建议不落库），完全离线 |

| `formatter.py` | 一键排版微调：清理多余空格空行、统一标题编号、补首行缩进，输出变更清单 |
| `tts.py` | 朗读校对：Windows 用系统自带 SAPI，麒麟用 espeak-ng，逐句朗读帮你"用耳朵找错" |
| `ocr.py` | OCR 识别（可选功能）：调用 Tesseract 引擎，扫描版 PDF 每页 220dpi 渲染后识别；未装中文包时提示安装命令 |
| `backup.py` | 备份/恢复：打包前先把数据库日志落盘、可选 AES 加密、保留最近 20 份自动轮转；恢复前还会再做一次"防手滑备份"并校验文件完整。附件按体积上限随包（手动默认 30 MB、退出/定时自动备份默认 8 MB，均可在设置里改，-1=不限制）：装不下的附件写进包内 `manifest.json` 与 `excluded_attachments.txt` 清单、恢复时逐个报出"缺哪些、为什么、去哪儿补"，自动备份另记 `logs\backup.log`，绝不静默丢；旧格式备份包（没有清单）照常恢复 |
| `security.py` | 口令锁：PBKDF2 加密 12 万次迭代存储口令，防暴力破解；定位是"防误用"，不是涉密级加密 |

### 6.5bis 收文·归档·运维组（v1.6.0 新增）

公文实务是**收 + 发**两个方向；此前只有发文，"别人发给我的"完全管不住。
这一组补齐收文侧，并把"出问题怎么查"这条路修通。

| 文件 | 作用 |
|------|------|
| `receive.py` | 收文登记台账：来文字号**宽松**解析（外单位格式不受本单位约束，拆不出也不报错）、字段校验、统计聚合、Excel/CSV 导出。密级/紧急程度/文种三组取值**直接引用 registry**，避免两处清单漂移 |
| `archive.py` | 归档与保管期限：批量编档号（按年度流水）、标注**永久/30年/10年**、生成可签字盖章随卷的《归档移交清单》（复用 docxgen 公文体式）。在办件不允许归档——会造成"卷内缺件"，比不归档更难收拾 |
| `reminder.py` | 办理时限督办：只认"填了应办结日期且未办结"的件，**不做后台常驻轮询**（伤续航，且公文以"天"为粒度）。启动时算一次、点开才拉清单 |
| `xlsx.py` | xlsx 导出（**零新依赖**）：xlsx 本质是 ZIP + XML，用标准库 `zipfile` 手写最小 OOXML 即可。解决 CSV 的三个硬局限——无列宽表头样式、无多工作表、长数字被 Excel 转成科学计数法 |
| `dbhealth.py` | 数据库自检与维护：`quick_check` 低频跑（正常时**完全静默**）、手动 `VACUUM`+`REINDEX`+FTS 重建。VACUUM 前预检磁盘空间（峰值需约 2 倍库体积） |
| `diagpack.py` | 一键诊断包：环境/依赖版本、能力探测、各表**行数**、运行日志打成 ZIP。**绝不含公文正文**，生成前把"包含什么、不包含什么"逐项摆给用户 |
| `exporter.py` | 批量导出与移交包：按分类/标签/时段导出，`manifest.json` 与备份**同构**（将来"从移交包导入"可直接复用备份的恢复逻辑）。附件超限写进 excluded 并注明原因，绝不静默丢 |

| `logs.py`（主包根） | 运行期诊断日志：队列异步（不卡 UI）、有界轮转（2 MB × 5）、自身失败无害，**绝不记公文正文**（需要说明规模时用 `redact()`）。同时安装全局异常钩子与 Qt 消息处理器 |

---

## 七、ui/ —— 窗口服务大厅（界面层，20 个文件）

| 文件 | 你在软件里看到的对应物 |
|------|------------------------|
| `main_window.py` | **主窗口**：左中右三栏布局 + 顶部 12 个按钮工具栏 + 5 个菜单 + 状态栏（显示资料数、纠错库条数、数据目录、"完全离线运行"） |
| `editor_panel.py` | 中间的大编辑器：编辑/预览/输出预览三个标签页；左侧公文大纲树；Ctrl+F 查找替换；每 3 分钟自动快照 |
| `library_panel.py` | 左侧资料库：搜索框、分类树（可右键管理）、文档列表（排序、按类型过滤、批量操作） |
| `reference_panel.py` | 右侧面板：上半是纠错结果（红=确认错、橙=疑似错，可逐条替换或永久忽略）；下半是写作参考（双击插入） |
| `import_dialog.py` | 导入材料对话框：支持拖文件/拖整个文件夹，带进度条 |
| `compile_wizard.py` | 一键汇编向导：第 1 步勾材料并拖拽排序 → 第 2 步选模板填封面 → 第 3 步生成（Word / A4 PDF / A3 小册子 / 批量模式四选） |
| `template_editor.py` | 模板管理：左侧模板列表、中间三个参数页（页面与正文/标题样式/红头封面页码版记）、右侧**实时预览** |
| `dict_manager.py` | 词典与词库管理：词典、错别字对、常用句式、忽略名单四个标签页，支持 CSV 批量导入 |
| `correct_dialog.py` | 任意文档纠错（v1.4.0）：任意格式文件/粘贴文本 → 按类别底色标记 → 逐处/整篇修正 → 保结构导出 DOCX/TXT |
| `registry_dialog.py` | 发文登记台账窗口：登记表单（自动取号）、台账表格、统计页、CSV 导出 |
| `material_dialogs.py` | 附件管理 + 回收站：附件加挂/还原/彻底删除，软删除恢复 |
| `compare_dialog.py` | 文档对比窗口 |
| `feature_dialogs.py` | 功能对话框合集（684 行）：新建公文骨架向导、格式体检、跨文档批量替换、历史快照、相似查重、系统与安全设置、锁屏解锁 |
| `workers.py` | 后台线程：导入、纠错、汇编、渲染 PDF 等耗时活都交给它，界面永不卡死 |
| `icons.py` | 工具栏图标：全部是代码里内嵌的 SVG 矢量图，**没有任何图片文件** |
| `widgets.py` | 公文字体常量表（小标宋/仿宋/黑体/楷体/宋体）和下拉框等小零件 |
| `theme.py` | 全软件统一的颜色定义（错误红、警告橙……），深浅色主题都保证可读 |

---

## 八、tests/ 与 scripts/

### tests/ —— 质检科（63 个文件，1190 道考题）

| 文件 | 考什么 |
|------|--------|
| `test_parsers.py` | 6 种格式解析正确性；**50 个文件批量导入 <10 秒** |
| `test_corrector.py` | 纠错准确率；**精标词库抽 100 对 100% 识别**；词边界不误报 |
| `test_data_search.py` | 增删改查、分类树；**20 万字资料检索 <1 秒** |
| `test_compile_pdf.py` | 生成的 Word 结构正确；A3 小册子页序数学；两遍渲染页码正确 |
| `test_features.py` | 工具箱、文种骨架、快照、体检、查重、水印、口令锁、便携模式等 24 项 |
| `test_app_smoke.py` | 冒烟测试：首启种子导入、主窗口能开能关 |
| `test_p1/p2p3_features.py` | 历次版本升级的回归测试（防止改新功能弄坏旧功能） |
| `test_ui_guard.py` | 界面保底：设锁后能正常启动、切换文档前有保存提示、全文替换可撤销 |

### scripts/ —— 后勤部（20 个文件）

| 文件 | 作用 |
|------|------|
| `build_windows.bat` | 一键打包 Windows 版（产出 `dist\gwtool\gwtool.exe` + 便携版 zip） |
| `build_kylin_arm64.sh` | 在麒麟电脑上一键打包 ARM64 版 |
| `kylin_offline_wheels.sh` | 在有网的电脑上预先下载麒麟所需的离线安装包 |
| `gwtool.sh` | **Linux 启动器**：启动前检查架构与系统运行库，缺库时给出确切的 apt 安装命令并写诊断日志 |
| `install_kylin_run.sh` | .run 自解压安装包的安装负载：校验架构 → 装到 ~/.local/opt/gwtool → 写桌面入口 |
| `setup_windows.iss` | Inno Setup 安装包脚本（生成 setup exe） |
| `install_context_menu.bat` | 给 Windows 加右键菜单"用公文汇编助手导入" |
| `uninstall_context_menu.bat` | 移除上述右键菜单 |
| `gwtool.desktop` | Linux 桌面图标/开始菜单项 |
| `seed_data.py` | **构建期**生成种子库 seed.db：下载 CC-CEDICT 词典 + 用同音字算法生成 4 万条混淆对（只在打包前跑，运行期零网络） |
| `e2e_check.py` | 端到端自检：模拟真实使用走完 9 大流程，逐项报 PASS/FAIL |
| `api_commit.py` | 网络受限时改走 GitHub API 提交代码的备用工具 |

### .github/workflows/build.yml —— 云端流水线

推送 `v*` 版本标签时，GitHub 自动在两台云电脑（Windows x64 + 原生 ARM64 Linux）
上跑完 465 道考题、打包出 2 个离线安装包并发布 Release。

---

## 九、数据流：一次完整的工作过程

### 启动时
```
main.py → app.py
  ├─ 读 paths.py 决定数据目录
  ├─ 第一次运行？把 seed.db 的词典/纠错对导入数据库（仅一次）
  ├─ 设了口令锁？先解锁
  └─ 打开主窗口
```

### 导入一份材料时
```
拖文件进导入对话框 → ImportWorker 后台线程
  → importer.py 按扩展名选解析器（parsers/）
  → 统一转成 DocTree 结构
  → dao.py 存数据库（算内容指纹，重复自动跳过）
  → 左侧资料库列表刷新
```

### 一键汇编时
```
勾选材料 + 拖拽排序 + 选模板（compile_wizard.py 三步向导）
  → compiler.py 取出各材料的 DocTree 合并
  → docxgen.py 生成规范 Word（红头/目录域/奇偶页脚）
  → pdfrender.py 两遍渲染出 A4 PDF（真实目录页码）
  → booklet.py 重排出 A3 骑马订小册子
  → 保存到 我的文档\公文汇编输出\
```

### 纠错检查时
```
按 F7 → reference_panel 发给后台线程
  → corrector.py 三级流水线逐句扫描
  → 结果按置信度红/橙着色显示
  → 点"全部替换"从后往前改写（Ctrl+Z 可撤销）
```

---

## 十、"我想改 XX，去哪个文件"速查表

| 我想…… | 去改这个文件 |
|---------|--------------|
| 加一种可导入的文件格式 | `core/parsers/` 加解析器 + `core/importer.py` 登记扩展名 |
| 调默认页边距/字体/行距 | `core/template.py`（`default_template()`） |
| 改 Word 生成细节（红头/目录/页码） | `core/docxgen.py` |
| 改内置 PDF 渲染 | `core/pdfrender.py` |
| 加新文种骨架 | `core/skeletons.py` |
| 加精标纠错对/机构沿革 | `core/corrector_data.py`（改完重新生成 seed.db） |
| 加纠错上下文/标点规则 | `core/corrector_data.py` |
| 改格式体检规则 | `core/inspector.py`（`_check_*` 系列：引文/数字/一致性/文风） |
| 改收文字段或收文流程 | `db/schema.py` 的 `receive_register` + `core/receive.py` + `ui/receive_dialog.py` |
| 改归档规则/移交清单样式 | `core/archive.py` |
| 改督办提醒的提前天数 | `core/reminder.py`（`SETTING_DUE_SOON`，或界面上改设置） |
| 改导出的 Excel 样式 | `core/xlsx.py` |
| 改诊断日志的记录范围 | `gwtool/logs.py`（**务必保持"不记正文"**） |
| 改诊断包里包含什么 | `core/diagpack.py`（同时改 `preview_lines()`，用户靠它确认隐私） |
| 改移交包的结构/清单字段 | `core/exporter.py`（清单要与 `backup.py` 保持同构） |
| 改主窗口/菜单/工具栏 | `ui/main_window.py` |
| 改编辑器行为 | `ui/editor_panel.py` |
| 改汇编向导流程 | `ui/compile_wizard.py` |
| 加一个功能对话框 | `ui/feature_dialogs.py`（或新建文件） |
| 改数据库表结构 | `db/schema.py`（记得加迁移步骤）+ `db/dao.py` 加对应方法 |
| 改软件名/版本号 | `gwtool/__init__.py` |
| 改打包内容 | `gwtool.spec` |

---

## 十一、常见疑问

**Q：为什么有两个"数据库"文件？**
`gwtool/resources/data/seed.db` 是**出厂自带的只读素材包**（词典+纠错对），
首次运行时被搬运进用户数据库 `gwtool.db`。这样升级软件只需换素材包，
不会碰用户已录入的数据。

**Q：改了代码怎么验证没改坏？**
在项目根目录运行 `python -m pytest tests/ -q`，全部考题通过即合格（当前 300+ 道）。

**Q：程序会把数据传到网上吗？**
不会。全部功能离线，打包时甚至刻意排除了 Qt 的网络组件；唯一的例外是
OCR/Word 转 PDF 需要的本机软件（Tesseract、WPS、LibreOffice），它们也都在
本地运行。唯一联网的脚本是 `scripts/seed_data.py`（构建期下载词典）和
CI 流水线，都发生在开发阶段，与用户无关。

**Q：根目录那几个 PDF 是干什么的？**
开发期手工验证 PDF 导入的测试样张（测试正文/页码/多页/左边距探测四种场景），
代码中没有引用，属于可清理的历史文件。

**Q：代码总量多大？我该从哪个文件开始读？**
约 8900 行 Python。推荐阅读顺序：
`main.py`（38 行）→ `gwtool/app.py`（104 行）→ `core/model.py`（65 行）
→ `ui/main_window.py`（419 行），读完这四个文件就对全貌有数了。
