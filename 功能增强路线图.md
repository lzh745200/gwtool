# 功能增强路线图

> 基线：v1.6.0（core 约 40 模块、ui 约 20 模块；14 张业务表 + 3 张 FTS5 虚表；`SCHEMA_VERSION=6`；测试 1262 passed / 3 skipped）
> 定位：**完全离线单机**，零网络请求；Windows 10/11 x64 与麒麟 V10（Debian 11 + glibc 2.31 + Python 3.9）双平台
> 本文档由现状盘点推出，所有结论都带证据（`文件:行号`）或实测数字；已否决策略原样沿用，不重复提议。

---

## 一 结论摘要（先看这三句）

1. **主线最大的断点不在"少个功能"，而在"产物没有户口"**：汇编向导生成正式公文后，产物**既不进资料库、也不进发文台账**，同一份公文的标题/机关/日期要在两个窗口各填一遍。这是 P0 第一项。
2. **已有数据与已有能力存在"最后一百米"问题**：台账页内已有统计（`registry.summarize` + `dao.dispatch_monthly_counts`），但没有一屏总览；纠错/查重/体检结果只能看，不能跳原文、不能导出；`dictionary_fts` 建了却无人调用。
3. **本批增强不需要动依赖、也不需要动库结构**：P0 三项可在零新依赖、零 `SCHEMA_VERSION` 变更下完成（`dispatch_register.doc_id` 字段早已存在，只是从无写入方）。

---

## 二 系统现状速览（能力域与实测基线）

| 能力域 | 现状 | 关键实测数字 |
| --- | --- | --- |
| 纠错 | L1 精确词表 / L2 规则（易混词·标点·数字）/ L3 重复字 + 可选 L4 神经精排与 L5 语法纠错（ONNX，独立增强包，不随主包分发） | L1–L3 10.7 ms/千字；公文体 0.00 处/万字；混排 18.47 处/万字；精标集 99.4% |
| 汇编与生成 | 12 文种骨架；一键汇编（封面/红头/目录页码/版记）；docx + 内置 A4 PDF + A3 骑马钉 | `exporter.build` 1.5–2.1 s；内置 PDF 渲染走两遍法真实页码目录 |
| 格式体检 | 15 项文本级 + 6 项 docx 级（标题链/发文字号/成文日期/请示规则/引文/数字/一致性/文风；页边距/字体/字号/行距/页码） | 结果仅列表展示，无跳转、无销号 |
| 台账与档案 | 发文台账、收文台账（登记/查询/统计/导出）、归档与保管期限、移交清单、督办提醒 | 收文 3000 行列表 73 ms；批量归档 1000 行 197 ms（单事务 19 ms） |
| 资料库与检索 | 分类树/标签/类型/排序/FTS5+jieba 预分词全文检索/附件/回收站/历史版本 | 3000 篇下列表 18.8 ms、检索 12.5 ms、重建索引 2.4–3.4 s |
| 数据进出 | 导入 docx/doc/wps/txt/rtf/pdf/md/html + 图片 OCR；模板与词表导入（csv/tsv/json/tbx/xlsx/docx）；移交包导出；整库备份/恢复；发文与收文台账**只能导出** | 台账 xlsx 3000 行 272 ms、峰值 19 MB |
| 体验与设施 | 朗读校对、文秘工具箱、口令锁、水印、诊断包、数据库维护、空状态（仅资料库） | 50 个动作已全部配悬停提示 |

---

## 三 A 章：增强路线图

### P0 —— 工作流提效（直接减少文书每天的手工重复）

#### P0-1 汇编产物落库 + 汇编→发文台账回填（首个特性，详见第四章）

- **价值**：一份公文从"汇编完成"到"台账登记"目前要手工搬 3~4 个字段；且产物不在库里，事后检索、查重、体检、归档都找不到它。
- **场景**：文书用「一键汇编」生成红头正式件后，系统自动把产物登记为一条资料，并在完成提示里提供「登记到发文台账」；字号可一键取号，登记后台账与原文双向可达。
- **契合度**：`dispatch_register.doc_id`（`gwtool/db/dao.py:104`）与 `documents` 表都已存在；`registry.next_doc_no`（`gwtool/core/registry.py:80`）与 `registry.validate`（:193）可直接复用；向导已收集封面标题/单位/日期（`gwtool/ui/compile_wizard.py:257-264`）。
- **要点**：①成功即入库（标题=封面标题、tags 标记"汇编成果"）；②登记默认**只询问不自动**，避免台账被汇编产物污染；③以 `doc_id` 做幂等键。
- **约束与权衡**：`idx_dispatch_no` 不是唯一索引（`gwtool/db/schema.py:260`），同字号查重必须放应用层；入库失败不能把"生成成功"变成"整体失败"。
- **工作量**：M（2–3 天）　**验收判据**：见第四章 B7。

#### P0-2 台账批量操作（导入 / 批量改状态 / 批量改密级与保管期限）

- **价值**：现在台账只有"逐条点"，年度交接、状态批量流转全靠手；而台账**没有导入**，历史数据只能靠手敲。
- **场景**：年底把上一年度 Excel 台账批量导入；把一批"已印发"批量改为"已归档"；把某类来文批量改密级与保管期限。
- **契合度**：`core/registry.py` 已有 `export_csv`/`export_xlsx` 与 `csvsafe`（:123）——导入正好复用其反向处理；`dao.xlsx_read` 做 xlsx 只读；`registry.validate`/`receive.validate` 做逐行校验；批量写用 `dao.update_receive_many`（`gwtool/db/dao.py`）同款**单事务**写法。
- **要点**：①导入前给"预检报告"（行数/可导入/跳过原因），不做静默丢弃；②批量改状态复用现有状态白名单常量；③批量删除若要提供，必须二次确认并留备份。
- **约束与权衡**：CSV 导入要中和公式注入前缀（与导出侧 `csvsafe` 对称）；导入用"同字号 upsert"保证可重复执行；新增 DAO 方法不得逐行 commit。
- **工作量**：M　**验收判据**：刚导出的 CSV 再导入，行数与关键字段不变；批量改 2000 行 < 100 ms（单事务）。

#### P0-3 结果类列表「可用性三件套」（空态引导 + 点击跳转原文 + 一键导出）

- **价值**：纠错结果、查重结果、体检结果、参考列表目前**只能看**——没有空态引导、点一条不能回到原文、不能导出交给领导或留痕。
- **场景**：文书看着纠错清单逐条核，点一条直接定位到编辑器对应段落；把体检结果一键导出成 Excel 整改清单。
- **契合度**：资料库列表已落地空状态组件（`gwtool/ui/library_panel.py::_add_empty_hint`）可抽成公共件；导出复用 `core/xlsx.py` 的 `Sheet`/`write_xlsx`；纠错已有命名锚点与段落定位机制（`correct_dialog` 的锚点方案）。
- **要点**：①统一空态文案风格（说明下一步而非只说"无数据"）；②结果项携带 `doc_id` + 定位锚点；③导出列与屏上一致。
- **约束与权衡**：四类结果的数据结构不同（纠错是 Correction 列表、体检是 Finding 列表、查重是文档对），需适配器而非硬改各处；不得为了"能跳转"破坏"体检对未入库文件也能用"的既有能力。
- **工作量**：M　**验收判据**：四类列表在空结果时都有引导文案；导出条数 == 屏上条数且 xlsx 可正常打开。

### P1 —— 把已有数据与能力"用起来"

#### P1-4 统计看板（一屏总览）

- **价值**：主任要"今年发了多少、几种文种、还有几件要督办"，现在必须翻台账页并自己算。
- **契合度**：`registry.summarize`（`gwtool/core/registry.py:94`）、`dao.dispatch_monthly_counts`、`dao.receive_stats`、`dao.receive_monthly_counts`、`reminder.pending` 全部已存在——这是"聚合已有能力到首屏"，不是从零做统计。
- **要点**：①图表用 **QPainter 自绘**（零新依赖，也不引入 QtCharts）；②数值必须与台账页同源同口径；③空库时给引导而不是空图表。
- **约束与权衡**：不做后台常驻刷新（沿用"不做常驻定时器"的结论），打开时算一次；大库下聚合耗时需实测（现有 3000 篇基线远低于阈值）。
- **工作量**：M　**验收判据**：看板各项数值 == 台账页对应统计（机检字段相等）。

#### P1-5 深色主题 + 用户可调字号

- **价值**：长时校对与暗环境下的可读性；老花眼用户被迫用系统缩放糊弄。
- **契合度**：`gwtool/ui/theme.py` 只有 14 处颜色/字号常量与单个 `build_qss()`，改造面清晰；`settings` 表可直接持久化主题与字号。
- **要点**：①`build_qss(tokens)` 双套 token（浅色保持现状、深色新增）；②字号按设置注入 QSS 与编辑器字体；③保留 `MUTED`/`severity_color` 等语义常量名，避免调用方大面积改名。
- **约束与权衡**：深色下必须逐个清理硬编码颜色（工具栏图标描边色、severity 三色、空态灰）；切换主题要能即时生效且不丢未保存编辑内容；`app.py` 的 `setStyleSheet` 调用点只有一个，改造可控。
- **工作量**：M　**验收判据**：切深色后无浅色硬编码残留（用取值扫描断言）；字号设置重启后保持。

#### P1-6 汇编向导补封面要素（文种 / 密级 / 紧急程度 / 发文字号）

- **价值**：与 P0-1 配套——现在向导不收集这些字段，登记台账时必然返工补填。
- **契合度**：文种/密级/紧急程度的常量已在 `gwtool/core/registry.py`（:21-26）与 `receive.py` 中定义；`skeletons` 已有文种概念可复用清单。
- **要点**：①在向导第 2 步（模板与封面）追加 4 个字段；②只写进封面与模板变量，不改版式规则；③留空时行为与现在完全一致。
- **约束与权衡**：封面要素属于"版式内容"，新增字段必须确保不改变既有模板的排版结果（仅当用户填写时才写）。
- **工作量**：S（半天到 1 天）　**验收判据**：留空时生成的 docx 与改动前逐字节一致。

#### P1-7 台账↔资料库事项关联（可回链跳转）

- **价值**：登记收文时把来文挂到资料库原件上，之后从台账点开原文；也让"某份材料被哪件公文引用过"成为可查事实。
- **契合度**：`doc_id` 字段两侧都已存在但**无写入方**；跳转可复用资料库既有打开通路。
- **要点**：①登记对话框加"关联资料"选择器；②台账列表加「打开原文」（`doc_id>0` 才可用）；③`doc_id` 悬空时置灰并说明，不崩。
- **约束与权衡**：需要台账与资料库两侧的去向一致性；不做双向自动同步（避免"删材料顺带删台账"这类危险联动）。
- **工作量**：M　**验收判据**：关联后从台账可打开原文；删除材料后入口置灰且有说明。

#### P1-8 体检整改销号

- **价值**：体检报告目前是一次性纸单，整改是否落实无人跟踪。
- **契合度**：复用 `core/report.py` 的 XLSX 导出（:92）与 `Finding` 结构；状态可先存 `settings`（避免动库）。
- **要点**：①导出清单增加"处理意见/状态"列；②回读该清单后可标记销号；③列出未销号项。
- **约束与权衡**：回读清单要容忍用户手工编辑（列顺序变化、额外列）；状态存 `settings` 会让"跨机器"丢失，若要求跨机一致则需入库（届时按 schema 变更流程走）。
- **工作量**：M　**验收判据**：导出→填写→回读后，未销号项数与填写一致。

### P2 —— 储备（价值明确、优先级靠后）

| # | 增强项 | 说明 | 工作量 |
| --- | --- | --- | --- |
| P2-9 | 纠错可解释溯源 | 每条 L1–L3 命中标注来源（内置精标 / 用户词表 / 具体规则名），复用 `error_pairs.source` 与 `ruleset` | S |
| P2-10 | 词典「分词命中」（变体方案） | `gwtool/db/dao.py:1008` 明确"12 万词条免建 FTS，命中即时"，故**不做全量 FTS**；可行变体是只对用户新增/导入词条懒构建，检索时 FTS 优先、LIKE 兜底 | M |
| P2-11 | 密级联动水印与锁 | 按 `secret_level` 自动套水印或口令锁，复用 `core/watermark.py` 与 `core/security.py` | S |
| P2-12 | 督办轻量提醒 | 尊重"不做后台常驻定时器"的结论：改为打开台账/启动时按需计算 + Qt 原生桌面通知 | S |

---

## 四 B 章：首个特性实施方案 —— 汇编产物落库 + 汇编→发文台账回填

### B1 现状与根因（均为核实过的事实）

| 事实 | 证据 |
| --- | --- |
| 汇编产物**从不入库** | `gwtool/ui/compile_wizard.py:336 _start()` 生成到磁盘即结束，全文件无任何 `dao.add_document` 调用 |
| 台账与汇编**零联动** | `dao.add_dispatch` 全库仅被 `gwtool/ui/registry_dialog.py` 调用 |
| `dispatch_register.doc_id` **无写入方** | 字段已存在（`gwtool/db/dao.py:104`，注释"关联资料库文档（0=未关联）"） |
| 幂等键**无唯一约束** | `idx_dispatch_no` 是普通索引（`gwtool/db/schema.py:260`），去重必须放应用层 |
| 向导**未收集文种/密级** | 仅收集封面标题/汇编单位/落款日期（`gwtool/ui/compile_wizard.py:257-264`） |

### B2 目标行为（用户可感知的四件事）

1. 汇编成功后，产物**自动入库**为一条资料：标题=封面标题（缺省"汇编成果"）、`file_path` 指向产物、`tags` 标记"汇编成果"与来源份数、分类可用设置指定（缺省不分类）。
2. 生成完成提示改为一个**非模态完成面板**：列出产物清单，并给「登记到发文台账」按钮。
3. 登记对话框**预填**标题 / 发文机关（汇编单位）/ 成文日期（落款日期归一化）/ 发文字号（`registry.next_doc_no`），用户补文种、密级、主送后确认；登记成功回写 `doc_id`。
4. 台账列表增加「打开原文」（`doc_id>0` 时可用），台账与资料库双向可达。

### B3 数据流

```
选材/模板/输出  →  compiler + docxgen + pdfrender  →  产物落盘
        │
        └─(新增)→ dao.add_document(标题, 正文纯文本, blocks_json=汇编树,
                                  file_path=产物, file_type, tags, category_id)  → 返回 doc_id
        │
        ├─→ 完成面板（产物清单 + 「登记到发文台账」）
        │
        └─→ 用户确认登记 → registry.from_compile(...) 组装 Dispatch
                          → registry.validate 预校验 → dao.add_dispatch(doc_id=上一步)
                          → 提示给「打开台账」/「打开原文」两个去向
```

### B4 改动文件与函数

| 文件 | 改动 |
| --- | --- |
| `gwtool/ui/compile_wizard.py` | `_start()` 成功分支：新增 `_save_products_to_library()`；把现有 `info(self, "生成完成！…")`（约 :516，批量路径约 :450）替换为 `_finish_panel(products, doc_ids)`；新增 `_finish_panel`、`_register_from_compile` |
| `gwtool/ui/registry_dialog.py` | 新增 `RegisterFromCompileDialog`（复用现有 `Dispatch` 表单构件，预填并允许修改）；台账列表加「打开原文」动作 |
| `gwtool/core/registry.py` | 新增 `from_compile(...)`：封面元数据 → `Dispatch`（日期归一化、文种缺省），内部调用既有 `validate` |
| `gwtool/db/dao.py` | 新增 `find_dispatch_by_document(document_id)`（幂等判据）；确认 `list_dispatch` 结果透出 `doc_id` |
| `gwtool/ui/main_window.py` | 承接「打开原文」跳转（复用资料库打开通路）；新增设置项 `compile_result_category` 与 `compile_ask_register`（默认询问、不自动登记） |
| `tests/` | 新增回归（见 B6）；`test_ui_actions_smoke.py` 的模态屏蔽需覆盖新对话框 |

**本方案不改 `SCHEMA_VERSION`**：`doc_id` 字段已存在，幂等以它为准；不新增表、不新增列，无迁移风险。

### B5 幂等与边界（必须逐条处理）

1. **同一产物重复登记**：按 `doc_id` 判断是否已登记，已登记则走更新并提示"已更新"，不新增记录。
2. **同发文字号重复**：应用层查重（索引非唯一）；命中时提示"该字号已存在"，由用户决定是否继续。
3. **登记失败**：产物文件与资料条目均不受影响，提示"可在台账中手工补登记"并记日志。
4. **入库失败**（分类被删、磁盘满等）：不得把"生成成功"变成"整体失败"——产物路径照常给出，失败信息写入完成面板。
5. **`doc_id` 悬空**（资料被移入回收站或彻底删除）：「打开原文」置灰并说明原因，不抛异常。
6. **批量模式**：不逐篇弹登记框（避免 N 次模态），只在完成面板给一个"批量登记"入口，逐条预填、逐条确认。
7. **测试纪律**：新对话框必须被模态屏蔽覆盖（`QDialog.exec` 与 `QMessageBox` 的静态方法都要屏蔽，后者是 C++ 侧实现、只屏蔽 `exec` 拦不住）。

### B6 测试点（要求红→绿）

| 测试 | 判据 |
| --- | --- |
| `test_compile_saves_product_to_library` | 汇编成功后 `documents` 新增 1 条：标题=封面标题、`file_path` 指向产物、`tags` 含"汇编成果" |
| `test_register_from_compile_writes_dispatch` | 完成登记后 `dispatch_register` 有 1 条，`doc_id` 指向该资料，`status` 在该表白名单内 |
| `test_register_is_idempotent_by_doc_id` | 同一产物登记两次：记录数仍为 1，第二次为更新 |
| `test_register_failure_keeps_product` | `dao.add_dispatch` 抛错时：产物文件与资料条目仍在，提示含"手工补登记" |
| `test_dangling_doc_id_disables_open` | `doc_id` 指向已删除资料时入口不可用且不抛异常 |
| `test_batch_compile_does_not_pop_modal_per_item` | 批量路径模态调用次数 ≤ 1 |

### B7 验收判据（可机检）

1. 汇编一次后：`SELECT count(*) FROM documents WHERE tags LIKE '%汇编成果%'` 为 1，且 `dispatch_register.doc_id` 等于该资料 id；
2. 重复登记同一产物，`dispatch_register` 行数不变；
3. `ruff check .` 全绿；`pytest tests/` **不低于 1262 passed / 3 skipped 基线**（新增用例计入）；
4. `scripts/e2e_check.py` 仍 25 项全过（并在 e2e 中补一条"汇编→入库→台账回填"链路）；
5. 安装包体积增量 < 1 MB（本特性零新依赖，仅新增代码与文案）。

### B8 工作量与风险

- **工作量 M（2–3 天）**：向导改造约 0.5 天、登记对话框复用约 0.5 天、DAO/SQL 约 0.5 天、测试与回归约 1 天。
- **风险与对策**：
  1. 自动登记若默认开启会污染台账 → **默认只询问**，并提供设置项；
  2. 文种/密级缺失导致台账字段不全 → 由 P1-6 配套收口，或在登记对话框内置为必填；
  3. 批量模式弹窗过多 → 按 B5.6 处理。

---

## 五 明确不做（沿用既有结论，避免重复投入）

| 不做的事 | 理由（既有结论） |
| --- | --- |
| 在线教程/帮助中心、联网协同与多用户 | 违反离线红线与单机单用户定位 |
| 台账回收站 | 与现有回收站体系语义冲突，增量价值低 |
| 列表分页 / 虚拟列表 | 3000 篇列表 18.8 ms、收文台账 73 ms，无需 |
| XLSX 流式化与临时文件 | 3000 行 272 ms、峰值 19 MB 可接受；且不落临时文件是刻意的原子性设计 |
| 把 `CORE_TABLES` 扩成全表 | 会重演"新增一张表导致用户所有旧备份不可恢复"的事故 |
| 把静默降级一刀切改 `raise` | 79 处静默中约 55 处是正确的降级设计 |
| 大模型/云纠错、INT8 量化 | 离线红线 + 体积 + 麒麟适配；INT8 已被实测证伪 |

---

## 六 约束与权衡汇总（实现任何一条都必须守）

| 约束 | 具体要求 |
| --- | --- |
| 离线红线 | 零网络请求；所有候选不得引入在线服务 |
| 依赖与体积 | 零新依赖（含 Qt 插件）；图表用 QPainter 自绘；安装包增量控制在 1 MB 量级 |
| 平台 | Windows 10/11 x64 与麒麟 V10（Debian 11 + glibc 2.31 + Python 3.9）；PySide6 分平台版本不可混用 |
| 数据库 | 允许 `SCHEMA_VERSION+1` 走迁移链；`CORE_TABLES` 不动；旧库能开、旧备份能恢复（已有测试固化） |
| 测试基线 | 1262 passed / 3 skipped 不得下降；UI 新对话框必须屏蔽模态框，否则全量测试会挂死 |
| 设计纪律 | 批量写单事务、纠错层异常必须咽掉、不硬写 hiddenimports、文档与代码数字同步 |
