# 系统缺陷分析与改进实施记录

> 对应计划：`系统缺陷分析与改进计划.md`（原 `.workbuddy/plans/quantum-thunder-einstein-GARF_nAQ.md`，
> 已随本记录归档进仓库；30 项 · 6 批次）
> 基线：v1.6.0（`gwtool/` 76 模块；测试 1127 passed / 3 skipped）
> 收口：本机 **1262 passed / 3 skipped / 0 failed**；`ruff check .` 全绿；
> `scripts/e2e_check.py` 25 项通过；CI（Windows x64 + 麒麟 ARM64）4/4 job success
> 版本号**未变更**（沿用 v1.6.0，按计划"不改版本号"）

本文件是实施记录，不是方案复述：每一项都写清"原来错在哪、改成什么、怎么验的"。
计划本身连同其证据（文件:行号）见 `系统缺陷分析与改进计划.md`。

---

## 一 交付编排（功能一次做全，按批分次提交）

| 批次 | 内容 | 远端提交 |
| --- | --- | --- |
| B1 | 零风险修复 8 项（N1/N10/N2+U4/D4/S2/D3/P2/F3） | `fe5f6022` |
| B2 | 数据安全与核心体验 6 项（D1/F1/F2/D2/N11/U1） | `270d18b1` |
| B3 | 错误文案治理 2 项（N6+U2 / N9） | `f2c0fc52` |
| B4 | 响应性与崩溃修复（N4 / N3+P1 / P3） | `fb120745` |
| D5 | 迁移链单事务 + 拒绝降版本号（**单独提交**） | `8602499e` |
| B5 | 首次体验与可发现性 4 项（N7+U3/N8/U5/N12） | 见提交链 |
| B6 | 储备与收尾 4 项（P4/D6/U6/N5） | 见提交链 |

> 计划要求"触碰迁移链的改动单独成一次提交"，故 D5 从 B4 内拆出。

## 二 逐项实施要点

### B1 零风险修复

- **N1 主窗口超屏**：`resize(1360, 820)` 在 1366×768 办公本上底部被裁。改为按
  `availableGeometry()` 夹取（≤90%）+ `setMinimumSize(1024, 640)`，且屏幕比最小尺寸
  还小时让位给"不超屏"。
- **N10 中文桌面导出目录**：`paths.export_dir()` 原只认 `~/Documents`，麒麟中文桌面
  （`~/文档`）下会退回 `Path.home()`，导出散落在主目录根部。改为按序探测
  `~/Documents` → `~/文档` → `XDG_DOCUMENTS_DIR` → `Path.home()`。
- **N2 + U4 设置对话框无滚动区**：`SecurityDialog` 全类无 `QScrollArea`，720px 高在
  1366×768 上"清理附件/关闭"够不到。照抄 `receive_dialog` 的滚动区写法并夹取高度。
- **D4 xlsx 工作表名含引号**：属性拼接改用 `quoteattr`，导出的工作簿不再被 Excel 判为损坏。
- **S2 台账 CSV 公式注入**：以 `= + - @` 起首的值加前置 `'`（不动 xlsx 通路——inlineStr 本身安全）。
- **D3 e2e 备份往返**：原只断言备份文件存在。补"恢复 → 断言文档数与标题集合一致"。
- **P2 exporter 重名检查**：`seen: set[str]` 取代线性扫描；`exists()+is_file()` 合并为一次 `stat`。
- **F3 设置写失败留痕**：`set_setting` 失败不再静默，返回布尔并留日志。

### B2 数据安全与核心体验

- **D1 合法空库顶掉真库**：换库前只读比较包内与当前库的 `count(documents)`，异常降幅
  写入 `RestoreReport` 告警并要求二次确认。
- **F1 快照失败仍覆盖原文**：抽公共 helper，失败进 `result.failures`（"正文已改，但回滚快照未保存"）。
- **F2 备份列表/恢复静默降级**：损坏包以 `{"corrupt": True, "error": ...}` 出现在列表里；
  附件失败并入 `RestoreReport`。
- **D2 自检盲区**：新增三条只读交叉检查（FTS 行数 vs documents、附件悬空、快照悬空）。
- **N11 数据目录不可写**：`app_data_dir()` 加真实写入探测（Windows 上 `os.access` 不可靠），
  启动期 `OSError/PermissionError` 与既有 sqlite 诊断同构：写 `~/gwtool_启动诊断.log` + 可读提示。
- **U1 批量导入失败清单**：`ImportWorker` 把（文件名，原因）经信号回传，UI 列出前 8 条并可导出清单。

### B3 错误文案治理

- **N6 + U2**：新增 `ui/errmsg.py::friendly(exc, action)`，把异常翻译成**含下一步动作**的中文；
  原始异常只进日志。接入批量替换/查重/体检/参考面板与六个 worker。
- **N9 字体可观测性**：`core/fontcheck.py`（下沉到 core）+ 生成前探测写日志 + 生成完成提示
  + 字体下拉对缺失族名加提示。**注意**：计划原写"下拉文本加后缀（本机未安装）"，
  实施时改为**悬浮提示**——`currentText()` 会被直接写进模板字体名，加后缀会污染生成的 docx
  （这一处偏离是为了不引入新缺陷，代码内已注明）。

### B4 响应性与崩溃修复

- **N4 退出崩溃（真崩溃）**：`FnWorker/CompileWorker/PdfRenderWorker/BookletWorker` 四个类
  此前**没有 `stop()`**，`_wait_all_threads` 调它静默无效；10 秒后仍 `accept()` 退出时
  QThread 存活 → Qt qFatal（`0xC0000409`）。补齐 `stop()`，并在 `closeEvent` 的 `accept()`
  之前对超时线程 `terminate()+wait(1500)` 兜底（含 `_tts_worker`）。
- **N3 + P1 主线程冻结**：备份/恢复/定时备份、`exporter.build`、`dbhealth.maintenance`
  （VACUUM+全库重分词）、体检、体检报告导出、任意文档纠错导出、资料库删除、重建索引
  —— 共 10 处移入 `workers.FnWorker`；新增 `_run_bg` 统一入口与 `FnWorker.progress` 通道，
  状态栏回报阶段进度；`dao.rebuild_fts` 加 `progress_cb`；`_rebuild_fts` 补 try/except +
  失败提示 + **结果为 0 条时也显式说明**。
  - 实施中发现并解决：恢复备份的占用检查（`live_connection_threads`）只排除"当前线程"，
    把恢复放进 worker 后会被**主线程自己的连接**挡住（实测报"正被后台任务占用（MainThread）"），
    故恢复前先关闭主线程连接。
- **P3 批量归档单事务**：新增 `dao.update_receive_many`（executemany + 失败回滚），
  `archive.batch_archive` 改用它。逐行 commit 会留下"一半已归档"的台账，而归档是整批成立的语义。
  - 顺检结论：`core/batch.py` 的批量纠错是**逐篇独立**语义（单篇失败不影响其它），
    保持逐篇提交是正确的，未改。

### D5 迁移链（单独提交）

- 建表/迁移/索引/FTS 整段放进**显式单事务**（Python sqlite3 的 legacy 模式只为 DML 隐式开事务，
  DDL 各自 autocommit —— 不显式 `BEGIN` 就没有"整批回滚"可言）。
- `user_version` **只在全部成功后**写入；真故障抛 `MigrationFailedError`。
  旧行为：真故障只记一条 warning 就继续，最后仍把版本号顶格写成最新 → 缺列的库再也补不上。
- 库比程序新（`ver > SCHEMA_VERSION`）：先自动留档 `*_newer_v{n}.zip`，再抛
  `SchemaTooNewError` 明确拒绝，**不再改写版本号**。
- 初始化失败的连接当场关闭（旧实现每次重试泄漏一个句柄与一对 `-wal/-shm`）。

### B5 首次体验与可发现性

- **N7 + U3 首启不阻塞**：字体检从"构造期同步弹模态 + 只弹一次"改为**状态栏常驻角标**
  （缺几种写几种，点击展开影响与解决办法），在 `showEvent` 刷新。
- **N8 空态引导**：空库列表显示不可选中的引导项（① 导入 → ② 双击编辑 → ③ 一键汇编）；
  检索无命中给"换个关键词试试"；编辑器占位文案改为"先在左侧点导入"。
  - 实施中砍掉一个**不可达分支**：类型下拉的候选项由当前材料重建，选中类型无材料时会自动
    落回「全部类型」，因此不存在"被类型筛选筛空"这一种状态。
- **U5 悬停提示**：主窗口 18 个动作从 3 个有提示补到**全部 50 个动作/菜单项都有独立说明**，
  汇编向导 7 个关键控件、收文表单 7 个易错字段（拟办意见 vs 领导批示、密级、紧急程度、
  应办结日期、保管期限）各配一句。
- **N12 依赖快照**：CI 两个 job 各加 `pip freeze > artifacts/requirements-locked-<platform>.txt`
  并上传（Linux 快照特意放在 `pkgsrc` 之外——那是 .deb 的载荷树）。

### B6 储备与收尾

- **P4 增量重建索引**：新增 `fts_index_state` 表（schema **v6**）记录"上次按哪份 `text_hash` 分词"，
  重建时跳过未改动文档；`incremental=False` 可强制全量（状态表被改坏时的兜底）。
  为什么必须比内容哈希：文档改过后 FTS 行**依然存在**，只查存在性会把改过的文档当成已索引。
- **D6 移交包校验**：新增 `exporter.verify_package()`（条目齐全 + 字节数 + sha256 对 manifest）
  与菜单入口「校验移交包…」。移交是责任转移，包坏了必须在交付前发现。
- **U6 汇编选材筛选**：分类/标签筛选 + 全选/全不选/反选。筛选**只影响可见性**：
  勾选状态与汇编顺序存在 `_order`/`_checked`，被筛掉的材料不会掉出汇编清单；
  全选/全不选/反选只作用于当前可见项；拖拽排序按"可见段并回完整顺序"处理。
- **N5 SAPI COM 释放**：`TTSEngine.close()` 显式置空 COM 引用，`TTSWorker.run` 的 `finally`
  调用它（正常结束与中途停止都覆盖）；`list_voices()` 同样显式释放。

## 三 全量门禁抓到的既有回归（b1/b2/b3 提交后未跑过全量）

首次全量跑（1237 例）即暴露三处，均已修复并各自带回归测试：

1. **产品缺陷：L4 开关每次拨动都弹"未能保存到数据库"假告警。**
   `core/csc_neural.set_setting` 返回 `None`，而设置页按 `if not saved:` 判定 →
   每次拨动都提示"重启后会恢复"，而设置其实写成功了。同族的 `csc_gec`/`reminder`
   已按布尔契约改造，只有这一处漏掉。
2. **真实模态框把无人值守测试挂死（两处）**：`test_corrupted_db_startup_guard` 走到
   `_startup_fatal` 的 `QMessageBox.exec()`，全量跑停在此处、日志十几分钟不增一行、
   无任何报错（`pytest-timeout` 栈可直接指认）；`test_settings_pack_groups.py` 同因。
   修法：屏蔽模态执行并断言"确实给了可读提示"；顺带把诊断日志从用户主目录重定向到 `tmp_path`。
3. **断言停留在旧契约**：`test_fn_worker_closes_on_exception` 仍断言原始异常文本
   `"boom"` 出现在 `failed` 信号里，而 b3 已把界面文案改为中文话术（原文只进日志）。

## 四 验证方式（每项都要求"红 → 绿"）

新增回归用例集中在 `tests/test_v170_batch_b.py`（`N1/N2/N3/N4/N7/N8/U5/P3/P4/D6/U6/N5`
各一组）与 `tests/test_db_probe.py::TestMigrationHardening`（D5 四例）。
**每条断言都在修复前的实现上确认过失败**，做法分两类：

- 临时还原被测行为（改回同步执行 / 逐行提交 / 吞掉迁移失败 / 老式模态提示 / 不给提示 /
  强制全量重建 / 校验直接放行 / 去掉 `close()`），跑对应用例确认失败，再恢复；
- 无法还原的（如"筛选控件原本不存在"）按"旧实现下必然 AttributeError"论证。

过程中另有两个**测试自身**的坑，值得记下：

- `QAction.toolTip()` 在未设置时会**回落为动作文本**，于是 `bool(a.toolTip())` 恒真——
  U5 的判据一度写成这样（"照抄式断言"），改为"提示必须是独立说明（≠标题且长度足够）"后才真能红。
- 用 heredoc 承载验证脚本时，脚本里的 `\n` 被命令行的路径转换吃成字面 `/n`，
  `str.replace` 静默不命中，"红检"看起来通过其实没改到源码 —— 验证脚本改为落盘后执行，
  并对每个替换模式加 `assert 模式 in 源`。

## 五 明确不做（继承计划结论）

列表分页/虚拟列表、XLSX 流式化、WAL 调优、启动期预加载 jieba、改 `synchronous=NORMAL`、
把 `CORE_TABLES` 扩成全表、静默吞咽一刀切改 `raise`、在线文档/教程/FAQ、
为台账加"回收站"、主题跟随系统的完整适配。理由见计划 §五（均有实测依据）。

## 六 遗留与后续

1. **麒麟 ARM64 实机验收（6 步清单）仍未做** —— `麒麟ARM64完整实现方案.md` §五。
   本轮 N2/N10/N11/U5 中多项（滚动区、中文桌面路径、写权限诊断、深色主题下的提示）
   **恰好需要实机才能最终确认**。本机为 Windows，无法替代。
2. **安装包已重新产出并通过 Windows 侧真实验收**：CI 在 tag 指向最终提交后重建了
   两个安装包（`gwtool_setup_win64.exe` 123.0 MB、`gwtool_1.6.0_linux_arm64.deb` 174.7 MB），
   本机对 Windows 包做了**真实安装验收**（下载最新包 → 静默装到临时目录 → 跑
   `scripts/acceptance_install.py` → 卸载清理）：关键资源齐全（exe / `_internal` /
   seed.db / 内置 Tesseract / chi_sim / 卸载器）、启动存活 12.1s、首启动种子导入纠错库
   40220 条 —— **0 项失败**。麒麟 ARM64 的 .deb 只经过了 CI 侧组装与产物冒烟，
   尚未在麒麟机上实装。
3. **依赖快照对比**：CI 首次产出的两份 `requirements-locked-*.txt` 需要人工对比一次，
   把确有差异的传递依赖在 `requirements.txt` 里显式加约束（N12 的后半截）。
